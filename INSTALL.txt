ASK CRUMP 5.2.4 — CI CLEANUP

WHY THIS EXISTS
The 5.2.4 feature deployment is valid, but GitHub CI found:
1. Two unused Python imports.
2. crump-5.2.4.js was missing from the JavaScript allowlist.
3. The 5.2.4 package accidentally removed Capacitor/RevenueCat dependency declarations from package.json.

HOW TO APPLY
1. Copy the CONTENTS of this folder into the ROOT of CRUMP-AI.
2. Choose Merge/Replace.
3. In the CRUMP-AI root folder, double-click:
   APPLY_5.2.4_CI_FIX.bat
4. Wait for "Python lint cleanup applied successfully."
5. The .bat file deletes itself after completion.
6. Open GitHub Desktop. You should see the cleanup changes.
7. Commit and push.

Suggested commit summary:
Fix 5.2.4 CI checks and restore native dependencies

No Supabase migration.
No Vercel environment changes.
No Stripe changes.
No application feature changes.
