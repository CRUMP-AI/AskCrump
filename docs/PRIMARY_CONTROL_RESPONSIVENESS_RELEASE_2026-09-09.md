# Primary control responsiveness release — 2026-09-09

## Outcome

Every reviewed Ask Crump button construction site remains owned by an explicit behavior, and the
complete credential-free browser control matrix remains green. A separate signed-in production
audit exercised the six primary destinations and their close controls without creating, deleting,
generating, downloading, purchasing, or changing customer records.

That production audit also found one narrow real-browser discrepancy that the isolated fixture did
not reproduce: **Think with Crump** left keyboard focus on its launch card instead of moving into the
message composer. The command now focuses the existing composer synchronously with
`preventScroll: true`. It does not move the page or conversation, add text, send a message, start a
generation, create a conversation, or consume credits.

## Automated acceptance

- The fail-closed inventory accounts for **181** rendered and **94** programmatically created
  buttons: **275 reviewed construction sites**.
- The ownership and button-state tests passed **21/21**.
- The complete browser control matrix passed **35/35**, covering account entry and recovery,
  navigation and mobile drawers, Chats, Create, Projects, Files and foreground viewers, generated
  output actions, Image Studio and Precision Edit, image stability, Video and reference images,
  Library, Settings, plans and credits, lifecycle and referral controls, attribution, and
  close/back/retry behavior.
- The first-action proof exercises all six launch choices and now has a source contract requiring
  the Think path to focus synchronously while the shared focus helper retains `preventScroll`.
- Full Python suite: **934 collected**, **932 passed**, two environment-dependent skips.
- JavaScript integration contract: **49 files** and **6/6** attribution cases passed.
- Ruff, Python compilation, production preflight, and diff integrity passed.
- The lightweight web worktree does not contain generated Android/iOS project directories, so the
  local native verifier correctly stopped at that boundary. Hosted Android and iOS source
  verification ran from the complete release checkout and passed.

## Production identity

- Commit: `c0dd8266ac78000a18696fc42e4e08fd8e5dc6cf`.
- Deployment: `dpl_5595FKb9yNcgfDg89grVM5Kh8vLv` — READY on all six aliases with no alias error.
- Main CI `34383842408`, Android verification `34383842324`, and iOS source verification
  `34383842250` completed successfully.
- Canonical `/api/health` returned HTTP 200. The live runtime exposes
  `5.9.76-control-focus-integrity-1`, and the deployed body contains the synchronous focus path and
  no deferred equivalent.
- A fresh signed-in production tab proved **Think with Crump** focused `#userInput` while both page
  and conversation scroll positions remained unchanged.
- The production browser emitted no warning or error during the focused replay, and the first
  deployment-scoped HTTP 5xx query was empty.

## Evidence boundary

This proves owned control construction, deterministic browser behavior, deployment delivery, and
one signed-in production focus replay. It does not claim that every external provider, browser
permission, payment, download destination, or signed physical-device state has occurred. Those
outcomes retain their explicit provider, commerce, and store-release gates. Destructive and paid
actions were inspected through deterministic fixtures rather than performed against the account.
