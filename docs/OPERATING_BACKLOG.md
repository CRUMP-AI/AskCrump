# Ask Crump operating backlog

Last updated: 2026-09-01

## Operating standard

Ask Crump's north-star outcome is a user completing valuable work, keeping it, and returning
to continue it. Revenue and user growth should follow verified activation, durable value,
retention, and referral behavior. No acquisition spend should scale on impressions alone.

Every item below needs four things before it is called shipped: an accountable product
outcome, privacy and safety constraints, automated coverage, and production evidence.

## Verified releases

| Outcome | Evidence | State |
| --- | --- | --- |
| Sanitized product captures have a fail-closed clean-state proof | Commit **de96b14** hardens the fixed internal demo-account workflow without touching a live account. The service-role-only operator tool now verifies a generic profile, empty profile preferences, exact default workspace settings, and no user-owned rows across sessions, conversations, Files, Projects, manuscripts/Library, media/Video, Code, Intelligence/memory, usage/finance/analytics, or notifications/lifecycle before declaring the account recording-ready. A versioned, content-free JSON receipt can be created after inspection or reset; it never contains an account ID, credential, prompt, response, filename, billing identifier, or customer content, and never overwrites prior evidence. A migration-parity test fails if any future `public.users`-owned table is omitted from clean-state coverage. **12/12** focused tests, all **732 Python tests**, **47 JavaScript validations**, compilation, production preflight, and diff integrity passed. No backend secret was configured, so no live demo/customer/founder account was inspected or changed; no runtime deployment, analytics event, payment, credit, generation, campaign, or publication occurred. Evidence: **docs/DEMO_ACCOUNT_CLEAN_STATE_RECEIPT_2026-09-01.md**. | Workflow verified; live marketing blocker remains open until an authorized operator performs the guarded reset locally, preserves its receipt, and records only rights-cleared fictional material |
| Image Studio always returns users to a usable control | Commit **9ca479a**; deployment **dpl_8NwjGSHohcjBYfnWm6SRHM1UVbL7**. A signed-in user-eye walkthrough found that **Create → Images → Close** tried to focus the now-hidden Images chooser card, leaving the page body active while Ask was visible. Image Studio now restores an opener only when it remains connected, enabled, visible, and outside hidden/inert/assistive-technology-hidden surfaces; otherwise it focuses **Message Crump** on the next frame. A real browser fixture proves both the durable-opener and transient-opener paths while preserving modal entry, reference editing, invalid replacement, preview stability, identity guidance, and Escape with zero errors. Adjacent image-scroll, image-rejection-recovery, and desktop/mobile Create handoff proofs stayed green. All **727 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in production opened Image Studio with its Close control focused, then closed to `userInput` with Ask active, no studio, a visible composer, and no overflow. Cache **r190** and exact **5.9.76-image-studio-focus-return-1** assets returned 200; the READY six-alias release had no runtime-error cluster, 4xx/5xx log, or warning/error/fatal log. No upload, message, generation, credit, Project, file, billing, analytics, account, or customer data changed. Evidence: **docs/IMAGE_STUDIO_FOCUS_RETURN_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat Close and Escape on exact signed iPhone/Android candidates with VoiceOver/TalkBack before store screenshots; observe legitimate image creation and return behavior before claiming lift |
| Large private file libraries reveal work progressively | Commit **2b42332**; deployment **dpl_syJJ4MZLvKFFx4v48wFi73koTg5T**. A signed-in user-eye walkthrough first reconfirmed that private previews now open above Files and return correctly, then found that the 65-item library still created all 65 cards and 18 video elements in one mobile surface. Files now renders a predictable first 12 matches, reports exact shown/matching/total counts, and reveals up to 12 more on demand. Search, category, sort, refresh, and fresh entry reset the window; reveal retains keyboard focus; preview/download/storage behavior is unchanged. Desktop and 390×844 fixtures proved 12-of-15 → 15-of-15 disclosure, every reset, ordering, no overflow, and zero errors. Adjacent file delivery, studio isolation, Video, Create → Video, and six-destination guide checks stayed green. All **726 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in production proved 12 of 65 → 24 of 65, then 12 of 23 Images, preview-above-Files, correct return, and zero browser warnings/errors. Cache **r189** and exact **5.9.76-file-library-window-1** assets returned 200; the READY six-alias release had no 4xx/5xx, runtime-error cluster, or warning/error/fatal log. No file, Project, content, generation, credit, billing, analytics, or customer data changed. Evidence: **docs/FILES_PROGRESSIVE_DISCLOSURE_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat large-library reveal/search/filter/sort/preview/download on exact signed iPhone and Android candidates before store screenshots; observe legitimate Files return behavior before claiming lift |
| Presentation campaign attribution crosses registration exactly once | Commit **de60728** adds an analytics-excluded browser and application-level registration fixture for the exact `instagram` / `profile-link` / `presentation-proof-current` / `ig-feed` / `presentation` tuple. The real landing and auth runtimes preserve the first touch after a second valid campaign appears in the same tab; the real registration function and analytics writer produce one isolated `AccountCreated`; direct replay and pending-account retry remain one; the privacy-safe fixture cohort moves **0 → 1 → 0** around cleanup. Production-control and finance sentinels remain unchanged. The local browser intercept proves one request with no network account or analytics delivery. All **726 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. No production endpoint, account, customer table, live mailbox, analytics, migration, payment, content, or legitimate ledger was touched. Evidence: **docs/PRESENTATION_ATTRIBUTION_FIXTURE_RECEIPT_2026-09-01.md**. | Verified isolated transport/idempotency/cohort/cleanup proof; legitimate external registration, activation, useful outcome, retention, payer, and revenue evidence remain open |
| Image editing has a visible reference-image entry | Commit **5c11d86**; deployment **dpl_DGECUFiNbaqcWE1jydNZS3RS13FQ**. A signed-in user-eye audit first proved that the startup cover correctly blocks interaction and that Create an image opens once ready, then identified the real gap: Image Studio promised editing but required users to discover a separate attachment path. Image Studio now exposes one optional single-image picker, recognizes an attached reference, distinguishes Create without reference from Continue with reference, and gives honest person-identity and logo/text fidelity guidance. Invalid replacements are rejected before the current valid reference is removed; filenames never enter interpolated HTML; modal labeling, focus, Escape, and pressed-state semantics are explicit. A 390×844 real-runtime browser proof covered picker → private upload preview → reference ready → edit composer, plus invalid replacement preservation, with zero errors. All **724 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in production exposed the exact new controls and guidance without selecting a production file or sending a generation. Cache **r188** and exact **5.9.76-image-reference-entry-1** assets returned 200; the READY release window had 10 successful 200s, no 4xx/5xx response, no runtime-error cluster, and no warning/error/fatal log. No model, provider, safety threshold, credit, plan, entitlement, upload/storage boundary, analytics, database object, or customer data changed. Evidence: **docs/IMAGE_REFERENCE_ENTRY_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat reference upload/edit/replacement/rejection/download on physical signed iPhone and Android candidates, then observe legitimate diverse edits before claiming fidelity lift |
| Crump Credit packs expose one clear purchase action | Commit **c07cde9**; deployment **dpl_6L6wqoDSHxQEvZTS6z4LV58tay1v**. A signed-in user-eye audit found that every available pack was exposed as a button-like card containing a second button, while all three inner controls shared the name **Add credits**. Each visual card is now pointer-friendly but non-interactive to the accessibility tree; its one real button owns keyboard semantics and names the exact amount and displayed price. Opening and recovery preserve the accessible name, and the touch-card path remains one idempotent checkout attempt. A phone/desktop browser fixture proved three packs, zero nested interactive cards, three unique actions, keyboard focus, one blocked checkout attempt, restored labeling, and zero errors. The adjacent image-scroll and paid-plan-intent browser contracts remained green. All **722 Python tests**, **47 JavaScript validations**, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in production proved one button, no role, and no tab stop on every live pack without opening checkout; cache **r187** and exact versioned assets returned 200. The READY release window contained 19 successful 200s with no 4xx/5xx response, runtime-error cluster, or warning/error/fatal log. No price, catalog, balance, allowance, subscription, entitlement, provider, checkout endpoint, payment, analytics, or customer data changed. Evidence: **docs/CREDIT_PACK_ACCESSIBILITY_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat with VoiceOver and keyboard/screen reader on exact signed candidates, then observe a legitimate checkout and credit reconciliation before claiming conversion lift |
| Navigation, onboarding, and store release sources cannot drift apart | Commit **caf0c67**; deployment **dpl_G4rspSsPvsaUcU6Bpxk1XUtZ1u5r**. A currentness audit followed a marketing hold caused by old five-destination creative. Current product and store sources already described the six visible destinations—**Ask, Projects, Create, Video, Library, You**—but no single build gate compared navigation, the workspace guide, both store descriptions, both screenshot arrays, the capture README, and the hidden Code boundary. Production builds now fail on any exact order or visibility mismatch. The real-browser guide proof expanded from Video alone to all six steps/actions; desktop and 390×844 runs also proved direct Video and Create → Video, with zero errors. Signed-in production replayed every guide step and opened every real destination with the correct focused surface and zero overflow. All **721 Python tests**, **47 JavaScript validations**, production preflight, Python compilation, native web build, store metadata, signing-source controls, and diff integrity passed. The READY six-alias release returned canonical 200s with no runtime-error cluster, 4xx/5xx log, or warning/error/fatal log. No runtime, account, content, Project, file, generation, entitlement, credit, billing, analytics, API, database, or customer data changed. Evidence: **docs/DESTINATION_RELEASE_PARITY_GUARD_2026-09-01.md**. | Verified build/release guard and current signed-in web/PWA handoffs; keep legacy five-destination creative held; repeat on the exact signed physical-device candidates before capturing or approving store/marketing assets |
| Saved Project Continue returns users to the work, not a hidden control | Commits **9a47bfe** and **c7f934d**; final deployment **dpl_6KGNR9gCSdeevbk3V6gVB2NANSRp**. A signed-in user-eye audit found that a saved conversation and its Project context restored correctly, but keyboard focus remained on the hidden Projects destination after the workspace closed. The first one-frame repair exposed a later production navigation callback; the final handoff now returns through the shared Ask destination owner, suppresses stale opener restoration, loads the linked conversation, and focuses **Message Crump** without scrolling. A deliberate current-session **Leave** remains respected. Desktop and 390×844 real-browser proofs covered the exact chat, Ask handoff, composer focus, Project target preservation, prior Leave behavior, mobile dock sizing, and zero errors. All **720 Python tests**, **47 JavaScript validations**, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in phone-width production ended with `userInput` active, the Project workspace hidden, Project context active, a 146px dock, and zero overflow, without sending a message or changing durable data. The READY six-alias release serves exact **5.9.76-project-resume-focus-2** and cache **r186**; its window had 30 successful 200s, no 4xx/5xx, no runtime-error cluster, and no warning/error/fatal log. Evidence: **docs/PROJECT_RESUME_FOCUS_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat Continue on a physical iPhone with VoiceOver, safe areas, the software keyboard, backgrounding, and PWA relaunch before store screenshots; observe legitimate continuation before claiming retention lift |
| Project continuity cannot silently leak into an unrelated chat | Commit **e20d010**; deployment **dpl_7p5FcuRgRmYrSiLgcDrcAtDv3GMR**. A user-eye audit found that opening a Project overloaded one client value as both save destination and conversation intelligence, so later chats could inherit its instructions/files while the context chip was hidden on phones. Ask Crump now keeps save destination and chat context separate: browsing a Project never activates it in chat; deliberate Use/New Project chat/Keep actions do; an existing owner-linked conversation restores its Project; and a fresh or unrelated chat stays clean. Desktop shows **IN PROJECT · Project name**; phone shows a named strip above the composer with an explanation and one-tap **Leave**, which pauses context without deleting or detaching anything durable. The browser first confirms the owner-scoped relationship and the server provides owner-scoped fallback recovery; the content-free confirmation marker is removed before intelligence/provider handling. Real-browser proofs at 1280×760 and 390×844 covered browse-only isolation, unrelated/fresh requests, linked restoration, Leave, save-target preservation, phone dock sizing, and zero errors; eight adjacent Video/File/image/navigation/lifecycle flows remained green. All **720 Python tests**, **47 JavaScript validations**, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in production opened **Savannah Reading Series** without activating chat context, continued its real saved conversation into the exact named phone strip, measured 146px active and 100px inactive docks with zero overflow, exercised Leave, and restored explicit context without sending a message or changing durable data. The READY six-alias release serves exact **5.9.76-project-chat-boundary-1** assets and cache **r184**; its window had 41 successful 200s, no 4xx/5xx, no runtime-error cluster, and no warning/error/fatal log. No account, Project, conversation, file, content, generation, entitlement, credit, billing, analytics, separate API release, database object, or customer data changed. Evidence: **docs/PROJECT_CHAT_CONTEXT_BOUNDARY_RELEASE_2026-09-01.md**. | Verified web/PWA/backend delivery and native-bundle inclusion; repeat linked/fresh/unrelated follow-ups and explicit Leave/reuse on a physical iPhone with VoiceOver, safe areas, backgrounding, and the software keyboard before store screenshots; observe legitimate Project continuation before claiming retention lift |
| Video always names its save destination | Commit **13a9e84**; deployment **dpl_GjQzW3BiVNfy1Ab3brdqpEQhdmxQ**. A signed-in user-eye audit found that simply opening a Project made it the target for later video work, while the active-Project chip was hidden on the launchpad and phone layout. Video Studio now presents **Saving to Project · Project name** before generation, explains that the finished video will appear in both the Project and private Files, and offers one clear **Use Files only** action. That action removes the hidden Project target, confirms the Files-only destination, and is disabled while a generation or private reference upload is in progress so the target cannot change mid-request. The server-authoritative request still sends the selected Project ID when present; storage, attachment, generation, metering, and provider behavior are unchanged. Desktop and 390-by-844 real-browser proofs opened a real Project, moved directly to Video, verified the named destination, exercised Files-only, and found zero errors; six adjacent Project/Create/Video/reference/File browser flows remained green. All **716 Python tests**, **47 JavaScript validations**, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. After accepting the PWA update, signed-in production displayed **Savannah Reading Series** in the restrained destination card, exercised Files-only, and restored the Project choice. The READY six-alias release serves exact **5.9.76-video-project-destination-1** assets and cache **r183**, with no runtime-error cluster, 4xx/5xx log, or warning/error/fatal log. No account, Project record, conversation, file, content, generation, entitlement, credit, billing, analytics, API release, database object, or customer data changed. Evidence: **docs/VIDEO_PROJECT_DESTINATION_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat Project choice, Files-only choice, generation, completion, and Project attachment on a physical iPhone before store screenshots; observe legitimate Project reuse before claiming retention lift |
| Top-level studios never inherit stale Project navigation | Commit **442f07f**; deployment **dpl_CLJzHSSqq1JTEt15fX4x18LyWSG7**. A signed-in user-eye walkthrough reproduced Video Studio showing **Back to all Projects** after a Project detail had been opened, and direct destination switching could retain Project-detail state and its URL. Video, Library, and Manuscripts now clear only the Project back control, internal view marker, detail class, and route before applying their own identity; active Project selection and all private data remain untouched. The full release gate also found that a later Chats-shell refresh could remove the sidebar's accessibility guard behind Create, so the shared destination owner now marks the covered background and the Chats owner honors it until restoration. Desktop/phone real-runtime proofs covered all three studio transitions, Create → Video → Ask, exact focus and accessible state, Files previews/downloads, and zero browser errors. All **714 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in production repeated Project → Video and Create background containment after accepting the PWA update. The READY six-alias release serves exact **5.9.76-studio-section-isolation-1** and **5.9.76-destination-background-guard-1** assets with no runtime-error cluster, 4xx/5xx log, warning/error/fatal log, or browser warning/error. No account, Project, conversation, file, content, generation, entitlement, credit, billing, analytics, or customer data changed. Evidence: **docs/STUDIO_DESTINATION_ISOLATION_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat Project → Video/Library/Create and returns on a physical iPhone with VoiceOver, safe areas, and the software keyboard before store screenshots |
| Private files are findable instead of one long wall | Commits **2d7cb3f** and **b3a3cfe**; final deployment **dpl_9mjVqNTGosN7zsRPqrCVKTuymkbz**. A signed-in user-eye audit confirmed that the preview-layer repair was holding, then exposed the next return-to-work friction: a real account had 65 saved items but no search or ordering control. Files now offers accent-insensitive name/title/prompt/type search, newest/oldest/name sorting, live All/Video/Image/Document counts, synchronized pressed state, exact shown-versus-total feedback, and phone-safe stacked controls without changing storage. A fresh Files visit also clears a residual search/type filter while preserving the current-session sort, so returning cannot make saved work appear missing. A desktop/phone real-browser fixture proved filtering, ordering, an unmatched search, return-state reset, 16-pixel phone controls, no overflow, and zero errors. The signed-in release loaded 65 items split into **18 videos, 23 images, and 24 documents**; `Kindling` returned one intended PowerPoint, old/new ordering diverged correctly, both a real image and PowerPoint opened above Files, and closing/reopening restored all 65 items. All **713 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Production serves exact asset **5.9.76-file-library-usability-2**; the READY six-alias release has no runtime-error cluster, 4xx/5xx log, or warning/error/fatal log. No account, file, Project, content, generation, provider, entitlement, credit, billing, or customer data changed. Evidence: **docs/FILES_FINDABILITY_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat search/filter/sort/preview/download on a physical iPhone with VoiceOver and the software keyboard before store screenshots; observe legitimate return-to-file behavior before claiming retention lift |
| Create never traps the destinations it leaves visible | Commit **d2f19ad**; deployment **dpl_7fWJuQYhTiHp2aXYWgsytLBMByrK**. A signed-in user-eye audit found that Create visually preserved both the desktop rail and phone destination bar while marking their entire app container inert, so those visible controls could not act until Create was dismissed. Create now isolates only the covered Chats/workspace canvas and follows the same non-modal persistent-destination contract as Projects, Video, Library, and You. A real-browser desktop/mobile proof measured the desktop overlay/rail boundary at **94px**, the phone overlay/navigation boundary at **776px**, and the phone bar at **68px**; one Video action closed Create and opened Video with correct focus, then one Ask action restored the workspace, with zero browser errors. All **713 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in production loaded exact asset **5.9.76-create-destination-handoff-1**, repeated the one-click flow, and the READY six-alias release had no runtime-error cluster, 4xx/5xx log, or warning/error/fatal log. No account, content, Project, generation, provider, credit, entitlement, billing, or customer data changed. Evidence: **docs/CREATE_DESTINATION_HANDOFF_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; repeat on a physical iPhone with safe areas, VoiceOver, and the software keyboard before store screenshots |
| Mobile Chats never traps the persistent destinations | Commit **1ccc78b**; deployment **dpl_DfcwECVmo3YnTgKKnjGacavgz1dw**. A signed-in 390-by-844 production walkthrough reproduced that the visible destination bar could not act while Chats covered it. The repaired drawer and dimming layer now stop exactly at the safe-area-aware navigation boundary, so one Projects tap closes Chats and opens Projects. The deterministic browser fixture measured drawer/overlay bottom and navigation top at **776px**, proved a **68px** destination bar, opened Projects in one tap, and reported zero errors. All **712 Python tests**, **47 JavaScript validations**, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Production serves the exact **5.9.76-mobile-drawer-destinations-1** stylesheet and the READY six-alias release has no runtime-error cluster. No conversation, Project, account, file, billing, entitlement, model, credit, or customer data changed. Evidence: **docs/MOBILE_CHATS_DESTINATION_HANDOFF_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; confirm the safe-area and software-keyboard experience on a physical iPhone before store screenshots |
| Signed-in users have one direct product-support path | Commit **321fe6b**; deployment **dpl_4rQFpWX5hsp8xu4WYLj6zRtg3CwU**. You → About now exposes **Product support** through the same `support@askcrump.com` address already configured for transactional email, while keeping legal, privacy, security, and abuse contacts separate. The link pre-fills only a generic subject and opens no email client until deliberate activation. All **711 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Signed-in production inspection proved the visible label, address, and exact `mailto:` target without activating it. The READY six-alias release serves cache **r181**, with no runtime-error cluster or warning/error/fatal log. No message, support request, account, session, content, checkout, payment, subscription, entitlement, or credit state changed. Evidence: **docs/IN_APP_PRODUCT_SUPPORT_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; complete a founder-controlled inbound delivery/triage test before advertising support-response expectations |
| Every account-entry password can be checked before submission | Commit **f9829c2**; deployment **dpl_4YpgUaoshgej5vLw8NCeBTZD9Gkw**. Sign-in, registration, new-password reset, and reset confirmation now share one accessible Show/Hide interaction while preserving the existing password policy, APIs, sessions, and verification flow. Controls are non-submitting, field-specific, focus-preserving, independently operable, and keep visible text, accessible name, and `aria-pressed` synchronized. All **710 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. A credential-free real-controller fixture proved masked → shown → masked behavior with zero auth events/errors. Production proved current-password and both independent reset controls without submitting any form or retaining dummy text. The READY six-alias release serves cache **r180** and exact **5.9.76-auth-entry-polish-1** assets, with no auth-route runtime-error cluster or warning/error/fatal log. No account, email, verification, password, reset request, session, content, checkout, payment, subscription, entitlement, or credit state changed. Evidence: **docs/ACCOUNT_ENTRY_PASSWORD_VISIBILITY_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; observe legitimate registration, verification, sign-in, and recovery outcomes before claiming activation lift |
| Account deletion cannot orphan an open web subscription | Commit **a255695**; deployment **dpl_41ryMztKTU3GrTNpobdZrDzEL2iK**. Ask Crump now deletes and exactly verifies the Stripe customer before its existing atomic local-account deletion whenever account state indicates an open web subscription. Missing billing configuration, provider rejection, network failure, malformed confirmation, or mismatched customer identity leaves the account intact with a clear recovery message. Terminal subscriptions preserve best-effort privacy deletion, and native RevenueCat subscriptions are not misclassified as Stripe. The in-app dialog, deletion-help page, and legal section consistently explain immediate web cancellation, no automatic refund, and separate Apple/Google cancellation. All **709 Python tests**, **47 JavaScript validations**, compilation, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Production serves cache **r179** and exact **5.9.76-account-deletion-billing-1** asset; signed-in inspection displayed the warning without submitting deletion, and the READY six-alias release had no account-route runtime-error cluster or warning/error/fatal log. No real account, password, customer, subscription, payment, refund, entitlement, credit, or provider configuration changed. Evidence: **docs/BILLING_SAFE_ACCOUNT_DELETION_RELEASE_2026-09-01.md**. | Verified web/PWA/backend delivery and native-bundle inclusion; first legitimate paid-account deletion and signed-device store-cancellation evidence remain controlled acceptance gates |
| Subscription recovery prevents duplicate checkout and stale entitlement rollback | Commit **86676a6**; deployment **dpl_CQ9o4XzBwyKgUTDfDgD2M2QQ6zzr**. A non-entitled but still-open Stripe subscription now exposes one **Fix billing** path instead of fresh purchase actions, and the backend refuses to create a second subscription while the existing one needs management. Browser retries reuse a content-free provider idempotency identity; Checkout destinations are validated before return. Delayed subscription events retrieve current Stripe state, while events for superseded subscription IDs cannot roll a newer account backward. Catalog plan and effective entitlement remain separate, so a troubled plan stays identifiable without granting paid allowances. A real-runtime browser fixture proved one recovery action, zero checkout requests, one portal request, and zero browser errors; the normal state preserved two deliberate plan choices and one checkout request only after selection. All **702 Python tests**, **47 JavaScript validations**, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Production serves cache **r178** and exact **5.9.76-commerce-recovery-1** assets; the signed-in Plan center remained healthy, canonical health returned 200, and the READY six-alias release had no subscription-route runtime-error cluster or warning/error/fatal log. No real checkout, charge, customer, payment, subscription, price, tax, entitlement, or credit state changed. Evidence: **docs/SUBSCRIPTION_COMMERCE_RECOVERY_RELEASE_2026-09-01.md**. | Verified web/PWA/backend delivery and native-bundle inclusion; observe the first legitimate external checkout through provider-reconciled entitlement, then validate invoice/refund/dispute recovery and unit economics before scaling paid acquisition |
| Paid-plan review is deterministic and truthfully positioned | Commits **944ed98**, **119f725**, **15e1021**, and **e124be5**; final deployment **dpl_DdZu533vPvX3x3Qqwh9eVffuez4G**. Paid-plan URL intent is retained in page scope and retried until the matching consumer acknowledges it. The consumer reuses either live billing owner, prevents duplicate panels and telemetry, announces the exact selected plan before slow billing data returns, and reapplies the selected card after either renderer hydrates. Enterprise no longer claims organization suitability; all signed-in renderers describe the largest current individual limits and exclude team/admin/SSO/SLA/procurement/support claims. Prices, allowances, entitlements, providers, checkout, and credit behavior did not change. A 40-run browser matrix covered both plans, phone/desktop, ten late-consumer timings, a billing-owner handoff, delayed billing, and a post-ack card replacement with zero checkout. Ten signed-in production plan runs passed, then the final Enterprise panel showed one modal, one exact status, one selected card, the truthful summary, and zero organization claims. All **695 Python tests**, **47 JavaScript validations**, production preflight, native web build, asset parity, and diff integrity passed. The READY six-alias release had no runtime-error cluster or 4xx/5xx log. Evidence: **docs/DETERMINISTIC_TRUTHFUL_PLAN_REVIEW_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; deterministic review is closed, but credit truth, commerce lifecycle, legitimate external paid intent, and finance authority remain separate gates |
| Files and images open above their workspace context | Commit **1fe8e9a**; deployment **dpl_4Wr7VoFbmtvJPX8XzUg7xSQUkRR9**. The production defect was reproduced exactly: Files sat at z-index 10,000 while document/image viewers sat at 120, so previews opened behind the current window. Shared modal mounting now places previews above workspace panels, preserves in-app document/image handling, supports Escape, restores focus to the originating file, and removes the direct private-storage browser fallback. The Files list no longer leaks the undefined `toolIcon` text or code-fence-only titles. A real browser fixture and signed-in production inspection proved document and image viewers at z-index 120,100, 65 saved items rendered, correct dialog semantics, return to Files after close, unchanged `/app` URL, and zero browser console errors. The full Python/JavaScript/native/preflight gates passed, exact production assets matched, and the READY six-alias release had no runtime-error cluster. Evidence: **docs/FILES_PREVIEW_LAYER_RELEASE_2026-09-01.md**. | Verified web/PWA delivery and native-bundle inclusion; observe legitimate open/download/reopen behavior before claiming artifact retention lift |
| Evidence-led workflow guides replace campaign 404s | Commit **6c48206**; production deployment **dpl_EPHRiHFTBW8tRvw6ewdXXgQqPA7R**. Three first-hand editorial guides now return direct 200 responses with self-referencing canonicals, approved authentic proof, visible author/date/method/disclosure, one matched CTA, adjacent workflow links, Article metadata, and clean sitemap entries. Search and social guide entries retain the exact allowlisted campaign tuple through the capability/signup handoff without overwriting an earlier first touch or storing content. The retired PowerPoint audit spelling and Clever-host duplicates permanently redirect to the single Ask canonical. All **612 tests**, **47 JavaScript validations**, lint, compilation, production preflight, native web bundling, store metadata, signing-source controls, and diff integrity passed. Live guides, sitemap, CSS, and assets returned 200; the representative asset hash matched; redirects returned 308; the READY six-alias release has no runtime-error cluster or severe log. The in-app browser sandbox could not produce a rendered frame, so physical-phone overflow/legibility remains an honest action-time marketing gate. Evidence: **docs/EVIDENCE_LED_SEARCH_GUIDES_RELEASE_2026-08-30.md**. | Verified production delivery; social publication, Search Console action, physical-phone preview, legitimate activation/retention, and revenue outcomes remain pending |
| Desktop Chats stays available after reload | Commit **8212576**; deployment **dpl_Aw8dZYrWbGGfgtuxEV9UrYhZhqoP**. Desktop startup now clears the legacy persisted collapse value and opens Chats on every new page load while preserving the in-session Hide/Show control and the mobile drawer contract. A stale-preference desktop fixture proved expanded boot, in-session hide, and expanded reload; the matched mobile fixture proved open, selection, and automatic close. An authenticated production reload remained 292 pixels wide and fully visible across 24 samples over roughly 2.4 seconds with no browser errors. All **579 Python tests**, **45 JavaScript validations**, lint, production preflight, native web bundling, and diff integrity passed. The **READY** six-alias release serves cache **r162** and exact **5.9.76-desktop-chats-default-1** loaders with no runtime-error cluster. Evidence: **docs/DESKTOP_CHATS_DEFAULT_OPEN_RELEASE_2026-08-30.md**. | Verified web/PWA delivery and native-bundle inclusion; observe legitimate desktop use before claiming a retention or activation effect |
| Intelligence has one coherent job and conversation privacy follows the account | Commit **19a0b93**; deployment **dpl_BWiwJurc4HRjMDkuaRXav6CgzER8**. Intelligence is now organized around Response effort, Memory & privacy, Live knowledge, Quality review, and its existing paid Advanced Intelligence boundary. Retired Web/Image/Code quick controls and the legacy mode strip are absent; real research, Image Studio, attachment, and Create destinations remain. Crump Code is a separate destination but stays hidden in production until its provider/readiness gates are configured. A sparse server-only Supabase opt-out table makes per-conversation memory privacy durable across signed-in devices and fails closed when privacy cannot be confirmed. The same release gives Clever Crump the approved 180px gold C/magnifying-glass iOS icon and a dedicated install manifest. All **579 Python tests**, **45 JavaScript validations**, lint, production preflight, native web bundling, and diff integrity passed. Live authenticated inspection proved the hierarchy, absence of retired controls, hidden Code gate, exact versioned assets, empty browser console, and correct Clever Crump iOS metadata; the READY six-alias release had no runtime-error cluster. Evidence: **docs/INTELLIGENCE_ARCHITECTURE_PRIVACY_AND_IOS_ICON_RELEASE_2026-08-30.md**. | Verified web/PWA/backend delivery and native-bundle inclusion; owner must delete/re-add the existing iPhone shortcut because iOS caches installed icons; observe legitimate cross-device privacy use before claiming adoption |
| Locked Intelligence actions reach one real plan destination | Commit **6a469c4**; deployment **dpl_Ccn9bjeDeSpBLaYuVJKDVfZEQqAq**. The deterministic audit found that Intelligence opened the billing center directly and then its plan-intent listener opened it again, replacing the first dialog and duplicating plan-center telemetry. A brief slow-runtime window could also leave an early click with no billing owner. The repaired handoff reuses an already-connected plan center, coalesces early clicks behind the workspace-ready event, and restores retry after a bounded 15-second failure. The existing server-side Professional entitlement and deliberate checkout boundary are unchanged. A content-free real-browser fixture proved normal and repeated early-click paths with one open, one billing-status request, one consumed Professional intent, the correct highlighted plan, and zero browser errors. All **574 Python tests**, **45 JavaScript validations**, production preflight, native web bundling, store metadata, signing-source controls, and diff integrity passed. The **READY** six-alias release serves the exact **5.9.76-intelligence-plan-handoff-1** assets with HTTP 200, healthy no-store service, no runtime-error cluster, and no warning/error/fatal release log. Evidence: **docs/INTELLIGENCE_PLAN_HANDOFF_RELEASE_2026-08-30.md**. | Verified web/PWA delivery and native-bundle inclusion; observe a legitimate locked action → plan review → deliberate checkout or safe return before claiming conversion lift |
| Every workspace action has one clear destination | Commit **b65fb73**; deployment **dpl_8MjtK6oMgng3fNJkBUbFELrroA2N**; the redundant composer Tools menu and its seven-option shell were removed. Ask remains the default, Intelligence owns research/current information and answer quality, Create owns Documents/Presentations/Images/Manuscripts/Video, the composer **+** owns attachments, and Crump Code remains a separate hidden gated destination. Hidden legacy mode buttons remain only as compatibility plumbing. All 569 Python tests, 45 JavaScript validations, production preflight, native web build, and a credential-free real-browser fixture passed. The READY six-alias release serves cache **r160** and exact **5.9.76-destination-tools-1** assets; inspected live assets returned HTTP 200, with no runtime-error cluster or warning/error/fatal deployment log. No production user, event, content, payment, model run, or entitlement was changed. Evidence: **docs/DESTINATION_TOOLS_RELEASE_2026-08-30.md**. | Verified web/PWA delivery and native-bundle inclusion; preserve the hierarchy and observe legitimate destination use before claiming activation or conversion lift |
| Advanced Intelligence proves when its extra work ran | Commit **8116d33**; deployment **dpl_3y1MtRWGDouV4TxxA7xhdsQuqYWK**; newly generated answers now persist and render **Thought longer** only when the planner returned a usable checklist and **Reviewed** only when the verifier returned a result. The sync boundary retains only two literal-boolean flags and excludes prompts, answers, plans, memory, provider/model details, tokens, cost, and hidden reasoning. User messages and string/number lookalikes cannot render the receipt. All 567 Python tests, 45 JavaScript validations, production preflight, native build, and desktop/iPhone real-Edge checks passed. The READY six-domain release serves cache **r159** and exact `5.9.76-intelligence-receipt-1` assets; all inspected canonical hosts and health returned HTTP 200, and the release window had no runtime-error cluster or warning/error/fatal log. No production model call, message, artifact, account, payment, checkout, or credit event was created. Evidence: **docs/ADVANCED_INTELLIGENCE_RECEIPT_RELEASE_2026-08-30.md**. | Verified web/PWA/backend delivery and native-bundle inclusion; this makes paid value visible but does not establish conversion lift—acquire legitimate external traffic and observe a real useful-result journey |
| Paid Advanced Intelligence is explained consistently before purchase | Commit **4407732**; deployment **dpl_FXc2HxuN8cr5boK4m9Ej4tswadAH**; public pricing, both signed-in plan renderers, the fallback plan renderer, and paid-plan registration now describe **Advanced Intelligence: Think Longer + Always Review** for Professional and Enterprise. Professional retains premium creation access, Enterprise retains Cinematic video access, and the existing no-purchase-before-confirmed-checkout disclosure remains intact. Prices, quotas, entitlements, credits, and checkout behavior did not change. All 563 Python tests, 45 JavaScript validations, production preflight, native build, and desktop/iPhone Edge checks passed. The READY six-domain release serves cache **r158**, all inspected live surfaces returned HTTP 200, and the release window had no runtime-error cluster or severe deployment log. Evidence: **docs/ADVANCED_INTELLIGENCE_PLAN_VALUE_RELEASE_2026-08-30.md**. | Verified web/PWA delivery and native-bundle inclusion; current aggregate account, artifact, and plan activity remains zero, so acquire legitimate external traffic before claiming conversion lift or changing pricing |
| Registration explains real Free value and the canonical product | Commit **19485fb**; deployment **dpl_FBTQeu7azo7zjwUs47niG1ZpVLpe**; signed-out registration now teaches **Ask, Projects, Create, Library, and You**, states the backend-enforced Free allowance of 25 messages each day and two private Projects, and tells Professional or Enterprise prospects that account creation does not begin billing and checkout remains separate. A write-blocked production browser audit proved generic Free at 320 by 667, Projects at 390 by 844, and Professional at 1,440 by 900 with focused email entry, above-fold primary actions, zero horizontal overflow, and no browser errors or unexpected failed requests. All 560 Python tests, 45 JavaScript validations, lint, compilation, production preflight, and diff integrity passed. The READY six-domain release serves cache `r156`, canonical health is HTTP 200/no-store, and the release window had no runtime-error cluster or severe deployment log. No production account, event, message, artifact, Project, checkout, payment, or subscription was created. Evidence: **docs/REGISTRATION_VALUE_CLARITY_RELEASE_2026-08-30.md**. | Verified web/PWA delivery; observe legitimate external first visit → registration → useful work → durable keep or return before claiming conversion lift |
| Crump Code has a fixed, provider-neutral quality gate | Fixture commit **f75ae5c**, evaluator commit **782f332**, and deployment **dpl_HruEkha3TKW13QGzKvUCZiDqv2WR** establish four pinned public cases spanning Python boundaries, JavaScript normalization, security-sensitive handling, and atomic concurrency planning. Exact path scopes, immutable acceptance files, canonical allowlisted receipts, one-attempt/duration/patch limits, strict artifact types, secret-pattern rejection, and categorical content-free reports fail closed. The evaluator cannot execute candidate code or select a custom suite, local artifacts stay uncommitted, and fixtures are excluded from production functions. All 12 evaluator tests, 559 Python tests, 45 JavaScript validations, lint, compilation, production preflight, native build, store metadata, signing controls, and diff integrity passed. The READY six-domain release returned healthy canonical pages and health with no runtime-error cluster, severe log, or Code event. No production task, model call, spend, credit event, or synthetic user activity was created. Evidence: **docs/CRUMP_CODE_FIXED_BENCHMARK_RELEASE_2026-08-30.md**. | Verified offline foundation; activation still requires one owner-approved no-secret live safety drill, separately injected holdouts, real provider runs, cost reconciliation, and repeatable results before any parity claim or enablement |
| Crump Code fails closed with observable, content-free operations | Commit **75a40fd**; deployment **dpl_zvBBwtqctrEgipnG2arUbHftvfVz**; the disabled Code control plane now emits only fixed, bounded dispatch, worker, cancellation, retry, terminal, and refund signals. Account/task IDs, objectives, prompts, repository URLs, source, patches, outputs, receipts, secrets, arbitrary fields, unsafe values, and exception messages are excluded. The emergency-stop path blocks claims/model/Sandbox work while still settling an owed terminal refund; a refund lookup failure defers Code and returns the shared slot to manuscripts. All 547 Python tests, 45 JavaScript validations, full lint, compilation, production preflight, native web build, store metadata, signing-secret controls, and diff integrity passed. The READY six-domain release returned healthy canonical pages and health, protected feature/cron requests remained 401, the next authenticated cron returned 200, and the exact deployment had no runtime-error cluster, severe log, or Code event. No production task, model run, charge, refund, or synthetic event was created. Evidence: **docs/CRUMP_CODE_OPERATIONAL_SAFETY_RELEASE_2026-08-30.md**. | Verified disabled monitoring and emergency-stop contract; activation still requires one owner-approved no-secret live drill proving OIDC, signals, cancellation, refund, destruction, alert routing, and rollback timing |
| Confirmed Crump Code work survives request interruption without duplicate compute or charges | Commit `64743c2`; deployment `dpl_8Deq7hEPhphqGLHX3MxGHVXK5u3a`; the disabled Crump Code foundation now accepts confirmed work with an idempotent metered dispatch, executes through a private replay-safe Postgres lease, rejects stale worker ownership, retries only bounded transient failures without recharging, and settles terminal refunds through the existing idempotent ledger path. The browser receives HTTP 202 and follows durable status instead of owning the serverless run. The existing manuscript cron is shared, so no third scheduled slot was added. Live Supabase checks proved both functions are security-invoker, fixed-search-path, service-role-only surfaces; anonymous/authenticated table and function access remains denied. All 540 tests, 45 JavaScript validations, lint, production preflight, native web build, store metadata, and diff integrity passed. The READY six-alias release serves cache `r155` and exact `5.9.76-code-durable-worker-1` runtime; all four customer-facing hosts and health returned HTTP 200, protected endpoints returned 401, and the settled deployment had no runtime error cluster. No production task, Sandbox, model run, charge, refund, or synthetic event was created. Evidence: `docs/CRUMP_CODE_DURABLE_WORKER_RELEASE_2026-08-30.md`. | Verified disabled web/PWA/backend foundation and native-bundle inclusion; activation still requires a real owner-approved no-secret run, benchmark/cost envelopes, live monitoring and kill-switch proof, and explicit enablement |
| Expired Crump Code work stops safely | Commit b77f1f4; deployment dpl_5gkEyG4fKFfGPMJsieK2x3vcrLU4; the disabled Crump Code foundation now terminalizes expired tasks and approvals, rejects late approval races atomically, blocks expired runs before claim or charge, and rechecks cancellation before Sandbox provisioning and around later expensive boundaries. The workspace exposes exact expiry state and keeps queued execution behind explicit confirmation. A credential-free real-runtime fixture proved expired, queued, and disabled states with zero browser errors. All 531 tests, 45 JavaScript validations, lint, production preflight, native web build, store metadata, and diff integrity passed. The READY six-alias release serves cache r154 and exact 5.9.76-code-lifecycle-expiry-1 runtime; all four public hosts returned HTTP 200, the unauthenticated feature endpoint returned 401, and the settled deployment had no warning/error/fatal runtime log or 5xx response. Crump Code remains disabled and no production task, approval, Sandbox, model run, credit charge, or synthetic event was created. Evidence: docs/CRUMP_CODE_LIFECYCLE_EXPIRY_RELEASE_2026-08-30.md. | Verified disabled web/PWA/backend foundation and native-bundle inclusion; activation still requires real no-secret Sandbox/OIDC/destruction proof, benchmarks, monitoring/kill-switch proof, and explicit enablement approval |
| Reloaded work remembers its selected Project | Commit `1f11cbd`; deployment `dpl_GWmhVMeNLZ3fW5BGjvR88zd6o5qE`; after an unsaved conversation reloads, the latest-result action now restores **Keep in “Project name”** through a lazy owner-scoped lookup returning only Project ID and name. An existing conversation-to-Project relationship remains authoritative and skips this lookup; generic startup still does not load Projects, Library, Create, or Files. A credential-free real-runtime fixture proved the slow pending state, exact restored save, saved relationship priority with no duplicate save, no-target creation, stale/unavailable recovery, coalesced requests, and zero browser errors. All 526 tests, 45 JavaScript validations, lint, production preflight, native web build, store metadata, and diff integrity passed. The `READY` six-alias release serves cache `r153` and exact `5.9.76-persisted-project-target-1` runtimes; all four public hosts returned HTTP 200, the unauthenticated target route returned 401, and the settled deployment had no runtime-error cluster, warning/error/fatal runtime log, or 5xx response. Evidence: `docs/PERSISTED_PROJECT_TARGET_RELEASE_2026-08-30.md`. | Verified web/PWA/backend delivery and native-bundle inclusion; observe legitimate Project reuse and return before claiming retention lift |
| Relationship-safe Project continuity | Commit `1b25f26`; deployment `dpl_EtYhTupsSj6SMeZxVLCst79AYQJi`; the latest-result Project action can no longer outrun the owner-scoped lookup that recognizes an already-saved conversation. It is disabled, busy, and labeled **Checking Project…** before the await, then resolves to **Open Project** or the correct unsaved save action while preserving fail-open recovery. A credential-free real-runtime fixture delayed recognition by 1.6 seconds: an interaction begun during the delay sent no save request for the already-saved conversation and opened the exact **Q3 Finance Forecast** workspace after recognition; the unsaved branch retained save/create behavior, each branch made one lookup, and browser errors remained zero. All 525 tests, 45 JavaScript validations, production preflight, native web build, and diff integrity passed. The `READY` six-alias release serves cache `r152` and exact `5.9.76-project-relationship-guard-1` UI runtime; all four public hosts returned HTTP 200, with no warning/error/fatal deployment log or 5xx response after settlement. Evidence: `docs/PROJECT_RELATIONSHIP_GUARD_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; observe a legitimate conversation-to-Project transition and return before claiming retention lift |
| Canonical Files handoff | Commit `1b68591`; deployment `dpl_Bdwc2GnxzfAq1hS25Av6XeZmmVEy`; the signed-in launchpad now teaches the canonical **Ask → Projects → Create → Library → You** model, and a stale cached Saved control opens Projects → Files instead of the books-and-manuscripts-only Library. The shared Files entry opens Projects safely from any caller, clears stale deep-Project routing, selects Files, and refreshes the owner-scoped file list. A credential-free real-runtime fixture proved the Files sheet and Projects panel visible, Library hidden, no book request, and zero browser errors; a separate markup check proved the exact five destinations and no Saved label. All 525 tests, 45 JavaScript validations, production preflight, native web build, and diff integrity passed. The `READY` six-alias release serves cache `r151` and exact `5.9.76-canonical-files-handoff-1` assets; all four public hosts returned HTTP 200, with no warning/error/fatal deployment log or 5xx response after settlement. Evidence: `docs/CANONICAL_FILES_HANDOFF_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; observe legitimate destination use before claiming activation or retention lift |
| Saved conversations remember their exact Project | Commit `71979fa`; deployment `dpl_N2Y4TVPWtTNViDu5sqtHhQ52iZgW`; Ask Crump now restores **Open Project** when a conversation already has an owner-scoped Project relationship after reload or cross-device return. The minimal authenticated lookup returns only Project ID and name, does not activate Project context, and fails open to the existing save action. A credential-free real-runtime fixture proved the saved path with one lookup, the exact named workspace, no duplicate save, and zero browser errors; its unsaved branch retained **Start a Project**, created exactly one Project, and transitioned correctly. All 525 tests, 45 JavaScript validations, production preflight, native web build, and diff integrity passed. The `READY` six-alias release serves cache `r150` and exact `5.9.76-durable-project-recognition-1` assets; the unauthenticated route returned the expected 401, with no warning/error/fatal deployment log or 5xx response after settlement. Evidence: `docs/DURABLE_PROJECT_RECOGNITION_RELEASE_2026-08-30.md`. | Verified web/PWA/backend delivery and native-bundle inclusion; observe a legitimate external save and later return before claiming retention lift |
| Chats language follows the product model | Commit `3ad9ffc`; deployment `dpl_9b6WGbeRPPXKodJvZPYFw2JMRMV2`; the product-wide destination-promise audit confirmed Library remains manuscripts and books, Projects → Files remains generated/uploaded work, and removed the remaining customer-facing “conversation library” language from the workspace shell. Desktop controls now announce Hide/Show Chats and the mobile menu announces Open/Close Chats from real drawer state; destination selection still executes before the mobile drawer closes. Credential-free real-runtime mobile and desktop fixtures proved state, semantics, and single execution without production writes. All 523 tests, 45 JavaScript validations, production preflight, native web build, and diff integrity passed. The `READY` six-alias release serves cache `r149` and exact `5.9.76-chats-language-1` assets with HTTP 200, no runtime-error cluster, and no warning/error/fatal deployment log. Evidence: `docs/CHATS_ACCESSIBILITY_LANGUAGE_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; preserve the legitimate-traffic observation gate and continue deterministic activation and retention work |
| Truthful Video signup destination | Commit `3ab94c0`; deployment `dpl_DD3AhukZY5xL3kfXbuYTaSw3eLaf`; the dynamic Video registration handoff now matches the public page, workspace guide, and released destination contract by directing completed clips to Projects → Files while Library remains books and manuscripts only. A real cold-entry controller fixture proved the exact Video intent, copy, focus, exploration link, and primary action before and after signed-out session resolution; the deployed app, versioned controller, and cache `r148` returned HTTP 200 with the expected contract. All 522 tests, 45 JavaScript validations, production preflight, and the native web build passed. No production signup event or account was manufactured. Evidence: `docs/VIDEO_SIGNUP_DESTINATION_HANDOFF_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; preserve the existing legitimate-traffic observation gate before changing authentication |
| Ask owns the conversation drawer | Commit `10130ad`; deployment `dpl_3LhJ5MMVqrxGz2aiaEBHzEG3mZrR`; the desktop rail now keeps Chats permanently discoverable as Ask's subordinate conversation drawer instead of a sixth primary destination. Ask alone carries the current-page state, Chats uses a quieter `is-open` state, and its accessible action changes between **Hide Chats** and **Show Chats** while collapse/reopen correctly controls the conversation panel's hidden and inert state. The local real-runtime fixture recorded zero browser errors, the signed-in production sequence passed and restored the original open state, all 522 tests and 45 JavaScript validations passed, production preflight and the native web build passed, and the `READY` six-alias release serves cache `r147` with only successful observed responses and informational runtime entries. Evidence: `docs/ASK_CHATS_HIERARCHY_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; acquire and observe the first comparable external first-visit → registration → useful-work journey |
| Truthful public destination map | Commit `306f6ad`; deployment `dpl_GMiiXaJZnbx9iheEAfZJbtsfKUAA`; the public product preview now teaches Ask, Projects, Create, Library, and You with the same boundaries as the signed-in workspace. Library is solely manuscripts and books, while generated and uploaded work is directed to Projects → Files across the home, document, resume, video, and parent-company pages. A visual browser pass verified the live hierarchy, exact-string checks found none of the replaced private-Library promises, all 521 tests and 45 JavaScript validations passed, and the `READY` six-alias production release serves cache `r146` with healthy canonical service and no observed 5xx. Evidence: `docs/TRUTHFUL_PUBLIC_DESTINATIONS_RELEASE_2026-08-30.md`. | Verified web/PWA delivery; acquire and observe the first comparable external first-visit → registration → activation journey |
| Measurable Plan-center recovery and credit conversion | Commit `283e500`; deployment `dpl_3tvSsSTw1G2QZ41uzMR21oXcUJu2`; Ask Crump now retains only fixed credit, subscription, feature, Project, or daily-usage recovery categories and records credit Checkout open/completion only from Stripe or RevenueCat server flows. Clients cannot submit those purchase milestones, analytics remain fail-open, and no content, balance, price, customer, card, or payment detail is retained. The service-role-only ten-stage aggregate report passed live privilege, security-invoker, empty-search-path, shape, allowlist, and index checks; one overlapping index was consolidated and the post-change advisors introduced no security finding. A credential-free browser proved all five recovery routes with zero checkout requests and zero browser errors. All 517 tests, 45 JavaScript validations, production preflight, native web build, store metadata, signing-source controls, and diff integrity passed. Production serves cache `r145` and exact `5.9.76-monetization-recovery-1` assets; the `READY` six-alias release has healthy canonical service, no runtime-error cluster, and no warning/error/fatal deployment log. Evidence: `docs/MONETIZATION_RECOVERY_MEASUREMENT_RELEASE_2026-08-30.md`. | Verified web/PWA/backend delivery and native-bundle inclusion; observe legitimate recovery → explicit checkout → provider-confirmed completion outcomes |
| Context-preserving Plan and credits recovery | Commit `0fa7cdb`; deployment `dpl_GJaumEcpgPn5EqTMzUwzsjxYdwaJ`; both live Plan-center owners now explain the exact structured reason they opened. Credit shortfalls show required credits and current balance without falsely implying a subscription upgrade, only a true subscription requirement can highlight Professional or Enterprise, and Project/daily-message limits keep their own fixed explanations. Raw server messages are not rendered and opening the center never creates checkout. A credential-free real-runtime fixture proved generic, credit, Enterprise, Project-cap, and daily-limit states with zero checkout requests and zero browser errors. All 504 tests, 45 JavaScript validations, production preflight, native web build, store metadata, and diff integrity passed. Production serves cache `r144` and exact `5.9.76-contextual-plan-recovery-1` assets; the `READY` six-alias release has healthy canonical service, no runtime-error cluster, and no error/fatal deployment log. Evidence: `docs/CONTEXTUAL_PLAN_RECOVERY_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; observe the first legitimate boundary → contextual Plan center → checkout or safe return outcome |
| Recoverable plan, credit, and Project-cap boundaries | Commit `63793f5`; deployment `dpl_7tuPryyC49ShuirGiUc8UEwaUpWg`; an API-to-interface audit found that explicit `CREDITS_REQUIRED`, `FEATURE_LIMIT_REACHED`, and several subscription failures ended as plain errors in chat, manuscript, export, and video creation flows, while the Project cap had the same dead end. The app now preserves structured server details, routes chat boundaries into the existing Plan & credits review, adds deliberate inline recovery beside manuscript/video/export failures, exposes recovery for durable manuscript runs waiting on credits, and covers both direct and conversation-created Project limits. Ordinary network/provider errors remain ordinary errors, and no checkout opens without a separate user choice. A credential-free real-runtime fixture proved exact 402 details, one chat recovery, one opt-in video recovery, no extra request, and zero browser errors. All 500 tests, 45 JavaScript validations, production preflight, native web build, store metadata, and diff integrity passed. Production serves cache `r143` and exact `5.9.76-feature-recovery-1` assets; the `READY` six-alias release had only HTTP 200 observations, no runtime-error cluster, and no warning/error/fatal deployment log. Evidence: `docs/FEATURE_ACCESS_RECOVERY_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; observe the first legitimate boundary → Plan center → checkout or safe return outcome |
| Generated work joins continuing Project context | Commits `46988f9` and `3c7705d`; deployment `dpl_49vUbumys2VH3HMAXmC6W48sNWts`; a deterministic activation audit found that keeping a conversation in a Project did not also place its generated document in that Project's Files section. The artifact card now keeps the source conversation and owned generated file together through the existing owner-checked, duplicate-safe Project route, then becomes **Open Project**; Download remains independent. A credential-free production-styled fixture proved ordered conversation/file handoff, the fixed `generated_document` role, success-state transition, retained Download, and zero browser errors without a production write. All 497 tests, 45 JavaScript validations, production preflight, native web build, store metadata, and diff integrity passed. Production serves cache `r142` and the exact `5.9.76-artifact-project-1` runtime/assets; the `READY` six-alias release has no runtime-error cluster or warning/error/fatal deployment log. Evidence: `docs/ARTIFACT_PROJECT_HANDOFF_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; observe the first legitimate request → package → Project/download journey |
| Announced, recoverable destination transitions | Commit `189e966`; deployment `dpl_FcA1gGNnkwf365XbACgC3QKipstE`; Projects, Library, and Settings previously opened visually while focus stayed on the triggering rail control. Their dynamic headings are now programmatically focusable, receive focus on entry, and leave existing in-sheet focus alone when a deeper Project flow owns it. Direct Close restores the exact visible desktop or mobile destination control, while explicit switches to Ask or Create retain composer and true-modal focus behavior. A credential-free production-controller fixture proved entry, destination switching, direct-close recovery, Ask/Create behavior, preserved background isolation, and zero browser errors. All 496 tests, 45 JavaScript validations, production preflight, native web build, store metadata, and diff integrity passed. Production serves cache `r141` and the exact versioned assets; the signed-in read-only flow passed on the `READY` six-alias deployment with an empty browser console and no runtime-error cluster or warning/error/fatal production log. Evidence: `docs/DESTINATION_FOCUS_TRANSITIONS_RELEASE_2026-08-30.md`. | Verified web/PWA delivery and native-bundle inclusion; physical-device keyboard and screen-reader passes remain |
| Persistent destination accessibility | Commit `508fabf`; deployment `dpl_2AcqJH4Jk4rTnTSyesYsHGNudFBY`; Projects, Library, and Settings previously claimed true-modal semantics while deliberately leaving destination navigation usable and keeping the covered conversation sidebar and main workspace reachable. They now identify as nonmodal dialogs, preserve and isolate only those two covered regions, leave the desktop rail and mobile destination bar interactive, and restore the exact prior state on Close or return to Ask. Create remains a separately contained true modal. A credential-free browser proved destination switching, direct-close recovery, Create focus wrapping, and zero browser errors. All 495 tests, 45 JavaScript validations, production preflight, native web build, store metadata, and diff integrity passed. Production serves cache `r140` and the exact versioned assets; the signed-in read-only production flow passed on the `READY` six-alias deployment with an empty browser console and no runtime-error cluster or warning/error/fatal production log. Evidence: `docs/PERSISTENT_DESTINATION_ACCESSIBILITY_RELEASE_2026-08-29.md`. | Verified web/PWA delivery and native-bundle inclusion; physical-device keyboard and screen-reader passes remain |
| Contained, recoverable Plan center | Commit `4c89e78`; deployment `dpl_GwJbA5qARwmW58QcXc6ERUa6ny6H`; the true Plan & credits modal previously left focus on its Settings trigger and kept 35 visible background controls reachable despite `aria-modal="true"`. Both live billing owners now move focus to Close, preserve and isolate visible background roots, dynamically contain focus inside the hydrated pricing sheet, close on Escape, and restore the exact opener and prior background state. A credential-free browser using the production controllers proved background isolation, Close ↔ Privacy wrapping, click/Escape recovery, the unchanged content-free Plan-center event, and zero browser errors without opening checkout. All 494 tests, 45 JavaScript validations, production preflight, native web build, store metadata, and diff integrity passed. Production serves cache `r139`, the versioned loader, and both versioned billing controllers; the release is `READY` on six aliases with no runtime-error cluster or warning/error/fatal deployment log. Evidence: `docs/PLAN_CENTER_FOCUS_CONTAINMENT_RELEASE_2026-08-29.md`. | Verified web/PWA delivery and native-bundle inclusion; observe the first legitimate checkout and provider-reconciled paid entitlement, then complete physical-device accessibility passes |
| Contained, accessible Create chooser | Commit `1589b7b`; deployment `dpl_5ESqFFUemiTWXjXZeLdK8azHdQB4`; the primary Create chooser announced a modal while leaving chats, navigation, and composer controls in the background focus order. It now preserves and applies `inert` plus `aria-hidden` to the workspace, loops keyboard focus across only the six dialog controls, and restores both workspace state and the exact Create trigger on close. A credential-free browser proved open, backward/forward wrap, close, restoration, and zero browser errors. The same read-only, non-generating sequence passed in the signed-in production workspace without creating or changing any record. All 492 tests, 45 JavaScript validations, production preflight, native web build, store metadata, and diff integrity passed. Production serves cache `r138` and the two versioned assets; the release is `READY` on six aliases with no runtime-error cluster or warning/error/fatal deployment log. Evidence: `docs/CREATE_DIALOG_FOCUS_CONTAINMENT_RELEASE_2026-08-29.md`. | Verified web/PWA delivery and native-bundle inclusion; physical iPhone PWA and exact signed-candidate accessibility passes remain |
| Parallel authenticated-workspace asset fetch | Commit `0083a1d`; deployment `dpl_zWcikDWvY5SY2ETcmkPNSU2Htz3Y`; Vercel Speed Insights showed `/app` at desktop RES 79 across 317 performance events while measured public routes scored 100. The loader serialized eight final styles and 29 scripts. It now inserts all 18 styles in final cascade order, primes all 29 scripts in parallel, and executes those scripts in the unchanged proven order. Under one credential-free 120-millisecond-per-asset fixture, runtime readiness moved from 5,060 to 299 milliseconds, with 18 concurrent styles, 29 preloads, the same first/last scripts, and zero browser errors. This controlled result is not a production RES claim. All 491 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Production serves cache `r137` and the versioned loader; a signed-in read-only load reached `ready` with 29 preload nodes and a complete workspace. The release is `READY` on six aliases with no runtime-error cluster or error/fatal deployment log. Evidence: `docs/PARALLEL_WORKSPACE_ASSET_FETCH_RELEASE_2026-08-29.md`. | Verified delivery; remeasure after seven post-release days or 300 post-release desktop performance events |
| Recoverable billing and payment return | Commit `a822fe7`; deployment `dpl_DwRLpxKCDfSvFf97pFWCQ6gRSSLD`; every active web billing layer now bounds the complete request and response-body parse to 15 seconds. A stalled credit checkout, subscription checkout, customer portal, billing-state request, or payment finalization can no longer disable a control forever. Timeout recovery restores the control with a clear retry message, and payment return clears Stripe parameters so refresh does not loop. A credential-free browser exercised the three final owners with one request and one abort apiece, restored both checkout controls, scrubbed the return URL, and recorded zero browser errors. No production checkout, payment, customer, or record was created. All 488 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Production serves cache `r136` and the three versioned timeout assets; the release is `READY` on six aliases with no runtime-error cluster or error/fatal deployment log. Evidence: `docs/BOUNDED_BILLING_REQUESTS_RELEASE_2026-08-29.md`. | Verified web/PWA delivery; observe the first legitimate checkout and provider reconciliation |
| Exact post-save Project handoff | Commit `08aae8d`; deployment `dpl_8EvqmG55Fu9LQZQMTiyvEEB5o85j`; after a useful answer is kept in a Project, **Open Project** now opens the exact returned destination instead of the generic Project index. The runtime keeps the owned Project identifier, opens known state immediately, and uses one bounded list refresh only when resolution is needed. A credential-free browser executed the real save/open path: one save request, the expected confirmation state, the exact named Project workspace, zero aborted saves, and zero browser errors. No production record was changed. All 485 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Production serves cache `r135`; the release is `READY` on six aliases with successful observed responses and no runtime-error cluster. Evidence: `docs/EXACT_POST_SAVE_PROJECT_HANDOFF_RELEASE_2026-08-29.md`. | Verified web/PWA delivery; observe the first legitimate external save-to-Project and later return |
| On-demand private-destination hydration | Commits `8ed0f64` and `d529287`; deployment `dpl_7GkVgrSJdsFsaNp4W7mEEsCPtEUw`; normal signed-in Ask startup no longer eagerly loads Projects, Library books/covers, Create availability, or Project Files. Each destination hydrates when intentionally opened, while direct owned-Project links still restore the named workspace and load its files only after the detail view is visible. Credential-free fixtures proved zero destination requests on ordinary startup and exactly one file request on direct Project restoration, both with zero browser errors. A signed-in read-only production trace proved the same boundary without changing any private record. All 485 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Production serves cache `r134`; the release is `READY` on six aliases with only successful responses/normal redirects and no runtime-error cluster. Evidence: `docs/ON_DEMAND_DESTINATION_HYDRATION_RELEASE_2026-08-29.md`. | Verified web/PWA delivery; observe the startup request boundary through ordinary traffic and confirm on a physical iPhone PWA |
| Books-only Library and separate Project Files | Commit `97d8298`; deployment `dpl_Ha1Uk36qG4d94J5vZDJCGKmdEuRE`; Library is now solely the private bookshelf for manuscripts and books, while documents, images, video, exports, and uploads remain available through Projects → Files. A content-free aggregate audit found existing files outside named Projects, so the release preserves the complete collection without migrating, attaching, deleting, or changing any record. Finished-video handoff, tutorial, store source, screenshot plan, PWA, and native bundle now teach the same boundary. Credential-free fixtures and signed-in read-only production checks passed at phone and desktop widths with zero overflow. All 485 tests, 45 JavaScript validations, backend compilation, production preflight, native web build, and store metadata passed. Production serves cache `r132`; the `READY` release is active on six aliases and its initial window contained 109 HTTP 200 responses, four normal redirects, no runtime-error cluster, and no warning/error/fatal log. Evidence: `docs/BOOKS_ONLY_LIBRARY_RELEASE_2026-08-29.md`. | Verified web/PWA delivery; observe legitimate book reopen and Files reuse outcomes, then confirm on a physical iPhone PWA |
| Measurable social profile-to-product handoff | Commit `d3d458a`; deployment `dpl_H1Ru89SL6aVweTfbcMADZwvid8Va`; read-only channel review found that profile-originated visits lacked one stable categorical placement before signup or sign-in. The site now accepts only the fixed `profile-link` placement alongside `response-share`, carries it with the separate Facebook/Instagram channel through both signup and sign-in, and drops invented placements. A credential-free browser proved both valid channels, the invalid-placement boundary, zero errors, and no production write. All 485 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Exact live assets contain the new placement and cache `r129`; the `READY` deployment had no 4xx, 5xx, or runtime error cluster. Detailed audience, funnel, account, and search measurements remain private. Evidence: `docs/SOCIAL_PROFILE_ATTRIBUTION_RELEASE_2026-08-29.md`. | Verified website delivery; tracked social profile edits, launch-batch publication, and sitemap submission remain owner-gated |
| Indexed Crump Code ownership and cleanup paths | Commit `9a7b482`; deployment `dpl_3qWoBXYnzpxUzv8ACDytxEY2FVyR`; Supabase migration `20260829223925`; the production non-success audit found only automated probes, deliberate validation probes, and three historical manuscript failures already corrected. Supabase had no warning/error and identified four informational unindexed foreign keys on the Crump Code event/approval ownership references. Four narrow btree indexes now cover `user_id` and `project_id`; catalog inspection found all four and the post-migration advisor reports zero unindexed foreign keys. A separate permission audit proved all 35 public tables have RLS, no browser-role CRUD grant exists, and the public view is not exposed. All 484 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. The deployment is `READY`, canonical health returned 200, and the initial release window had no runtime error cluster. Evidence: `docs/CRUMP_CODE_FOREIGN_KEY_INDEX_RELEASE_2026-08-29.md`. | Verified delivery; reassess index usage only after representative Crump Code traffic or material table growth |
| Event-driven credit balance instead of visible-tab polling | Commit `413a136`; deployment `dpl_Bnfx2Ekf22ZBdPJnTBdA6MrppGph`; the prior 24-hour aggregate contained 520 credit-status requests and the preceding hour contained 57, matching the explicit one-minute visible-tab loop. The badge now consumes exact server-returned balances from usage, successful/recovered replies, and purchases; startup signals coalesce, Plan Center remains authoritative on open, and focus/visibility return refreshes only after five minutes of staleness. A real browser proved one startup request, zero duplicate/fresh-return requests, immediate event-driven 12 → 41 balance, and exactly one stale-return refresh to 22 with zero errors. All 483 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Exact deployed assets contain the event handoff, versioned billing asset, and cache `r128`; the `READY` deployment's initial window contained two HTTP 200 responses with no runtime error or warning/error/fatal log. Evidence: `docs/EVENT_DRIVEN_CREDIT_BALANCE_RELEASE_2026-08-29.md`. | Verified delivery; complete 24-hour request-volume reconciliation pending |
| Addressable named Project workspaces | Commit `f98008d`; deployment `dpl_Anv4zcbudezZeMSg9sN9gp3wV7ok`; every Project row is now a real `/app?project=<owned-project-id>` link with instant in-place opening, refresh restoration, browser Back to the Project index, and a visible unavailable-Project fallback. Durable links are canonicalized to the app path plus only the Project identifier, so stale signup, campaign, verification, and arbitrary query parameters are not copied forward. A real-runtime fixture proved click, clean URL, refresh, Back, a 390-by-844 layout, and zero errors; signed-in production opened a real owner Project, restored it after refresh, and returned to the eight-Project index with zero browser errors and no writes. All 479 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Exact deployed assets contain the new route/cache markers; the `READY` release is active on six aliases, and its inspected window contained 124 HTTP 200 responses with no 4xx/5xx, warning/error/fatal log, or runtime error cluster. Evidence: `docs/ADDRESSABLE_PROJECT_WORKSPACES_RELEASE_2026-08-29.md`. | Verified web/PWA delivery; owner physical PWA confirmation and first legitimate external Project-value/return outcome pending |
| Public Projects acquisition and fixed workspace handoff | Commit `1e36c63`; deployment `dpl_EVUDxSqkkx64oNrFRdwJarNywkzA`; Ask Crump now has a canonical `/ai-project-workspace` page, a wide homepage discovery card, reciprocal links from every creation page, and a sitemap entry. Six fixed CTAs preserve only the categorical `projects` intent through signup/sign-in, then the ready authenticated runtime opens the existing Projects destination, records one content-free continuation event, acknowledges the handoff, and clears it without creating a Project or generating anything. A real browser proved the styled desktop page, 390-by-844 responsive layout, exact authenticated handoff, and zero browser errors. All 479 tests, Python lint/compile, 45 JavaScript validations, production preflight, native web build, and store metadata passed. The canonical page and exact changed assets returned 200; the deployment is `READY` on six aliases and its first runtime window contained two HTTP 200 responses with no warning/error/fatal log. Evidence: `docs/PROJECT_ACQUISITION_HANDOFF_RELEASE_2026-08-29.md`. | Verified delivery; first legitimate external Projects visit → account/workspace → durable value → return outcome pending |
| Registration consent handoff and reliable dedicated Project opening | Commits `b5283be` and `52f5e81`; deployment `dpl_w7qv72UP7SWXwbY99HJrq1jYMsEX`; current registrations record explicit age/Terms/Privacy consent with the published `2026-08-01` version before verification while retaining the authenticated fallback for legacy or missing consent. Project rows now use one stable delegated click path, normalize IDs, show a visible stale-row failure, and transition immediately from the index into a named dedicated workspace on desktop and mobile. An isolated browser proved required unchecked consent at 390 by 700, then proved Project open, Back, reopen, and phone-width layout without overflow or browser errors. Supabase reported eight active Projects with no missing IDs or names. All 479 tests, Python lint/compile, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Canonical health and exact registration/Project assets returned 200; the final deployment is `READY` on all six aliases and its first runtime window contained two HTTP 200 responses with no warning/error/fatal log. Evidence: `docs/REGISTRATION_CONSENT_HANDOFF_RELEASE_2026-08-29.md` and `docs/PROJECT_CLICK_RELIABILITY_RELEASE_2026-08-29.md`. | Verified delivery; first legitimate current-registration activation and owner desktop/phone Project-return outcome pending |
| Verified-email workspace handoff | Commits `165f37b` and `17a9700`; final deployment `dpl_H6EgjJW1KkUrq8ZLhrg52G4TN19v`; a valid verification link now verifies the account, creates the normal hardened web session, and opens the workspace without redundant password entry. First use narrows the original 24-hour token to a 15-minute replay window so mail scanners cannot silently consume the only usable GET link; invalid or expired links create no session, and password sign-in remains available for another device. The retired production `AI Virtual Assistant` label is normalized to `Ask Crump`. All 476 tests, live-path lint/compile checks, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Canonical health returned service `Ask Crump`; the final deployment was `READY` on all six aliases and its first window contained 22 HTTP 200 responses with no runtime error or warning/error/fatal log. Full evidence is in `docs/VERIFIED_WORKSPACE_HANDOFF_RELEASE_2026-08-29.md`. | Verified delivery; first legitimate verification-to-workspace activation and 14-day/50-social-visitor outcome boundary pending |
| Replay-safe manuscript scheduling | Commit `d9e3189`; deployment `dpl_86QGTpLX4duvhqngCqTeoKuWCUYY`; three isolated database 503 responses affected only `/api/cron/manuscripts` on an older deployment while Postgres showed no engine error or connection-exhaustion cluster. The worker now supplies one UUID to a private replay-aware lease function and opts only that idempotent RPC into bounded transient retries; ordinary writes remain single-attempt. Supabase migration `20260829184012` preserves security-invoker and service-role-only execution. All 472 tests, 45 JavaScript validations, explicit Python compilation, production preflight, native web build, and store metadata passed. Two natural post-deploy schedules returned 200 in both Vercel and Supabase; the exact release window contained only 200 responses with no non-informational log or runtime-error cluster. Full evidence is in `docs/REPLAY_SAFE_MANUSCRIPT_CLAIM_RELEASE_2026-08-29.md`. | Verified correction; 24-hour recurrence boundary pending |
| Context before referral signup and cache-routed Project workspaces | Commit `5ea65e2`; deployment `dpl_6fGicVAbJCimz9bSpiNPQGgecqZa`; response-share links no longer send a cold recipient straight to the registration wall. They open the public product page with only the aggregate `referral` channel and fixed `response-share` placement, and that allowlisted context survives a later signup or sign-in CTA while arbitrary source values, content, and identifiers do not. The already-corrected Project workspace runtime now has an explicit web/PWA/native asset identity so older sessions receive the named Project-opening interaction. Credential-free browser fixtures proved the referral rewrite and Project detail handoff with zero errors or production writes. Signed-in production opened `The Last Distance` with its files, conversations, and scoped-new-chat action. All 469 tests, 45 JavaScript validations, explicit Python compilation, production preflight, native web build, and store metadata passed. Canonical health and exact release assets returned 200; the first inspected window contained 40 successful responses with no deployment-specific 4xx, 5xx, warning, error, fatal, or runtime-error log. Full evidence is in `docs/CONTEXTUAL_REFERRAL_HANDOFF_RELEASE_2026-08-29.md`. | Verified delivery; first legitimate response-share → referral visit → account → activation outcome pending |
| Measurable Project conversation returns | Commit `3fa6f34`; deployment `dpl_GeCzd3eiXby5DUf1qruiGRfHwZFB`; the Project workspace client already emitted the content-free `RecentWorkResumed` event with source `project`, but the authenticated server allowed only `launchpad` and rejected Project returns with 422. The server now accepts exactly those two sources, preserves the historical launchpad key, and derives a separate UTC-day Project key so repeated clicks remain idempotent without mixing the return paths. No Project ID, chat ID, title, prompt, response, filename, URL, or arbitrary metadata is accepted. A pre-release read-only production aggregate contained two launchpad rows and zero Project rows. A non-writing production probe proved the approved Project event passes validation and stops at authentication with 401 while an invented source still returns 422. All 468 tests, 45 JavaScript validations, explicit Python compilation, production preflight, native web build, and store metadata passed. Canonical health returned 200; the exact release had no runtime error cluster, 5xx response, or warning/error/fatal log. Full evidence is in `docs/PROJECT_RETURN_MEASUREMENT_RELEASE_2026-08-29.md`. | Verified measurement; first legitimate Project return and comparable retention outcome pending |
| Dedicated Project workspaces and stable shell identity | Commit `a160ded`; deployment `dpl_AuCrCSecFTyrvi7AHLERExkHsEFG`; Project rows no longer silently repopulate an editor. Each row exposes an explicit Open action and transitions to a named workspace containing that Project's description, instructions, files, conversations, canon, durable notes, Back navigation, and a fresh Project-scoped chat action. New Project has a distinct create state, and phone layouts devote the sheet to the opened Project. The shell now uses a deterministic 1200-by-300 official “An AI workspace for work that continues” lockup outside the asynchronously restored Project-context node. The signed-in production lockup stayed at x=111/y=15.6667 and 200-by-50 pixels through 5.375 seconds. A real owner Project opened and returned to the eight-Project index correctly. All 467 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Canonical health and exact release assets returned 200; the inspected release window contained 54 HTTP 200 responses, no 4xx/5xx response, and no runtime error cluster. Full evidence is in `docs/PROJECT_WORKSPACE_AND_SHELL_LOCKUP_RELEASE_2026-08-29.md`. | Verified correction; owner physical phone/PWA Project navigation confirmation pending |
| Bounded idle synchronization | Commits `191a057` and `edba5dc`; final deployment `dpl_4krm9V8a67QhsdRLWFKj8qE9fqRc`; a read-only 24-hour production aggregation found 4,688 sync pulls and 2,345 sync pushes among 11,630 HTTP 200 responses. The client was performing a full pull, a complete local-state push, and another full pull every 20 seconds even when idle. The corrected contract queues writes before attempting the network, flushes them once with an account-scoped single-flight guard, preserves one startup migration reconciliation, and reduces the recurring path to one incremental visible-tab pull per minute with immediate foreground/reconnect recovery. A credential-free real-client fixture proved one idle GET and zero POSTs, one offline queue entry, exactly one reconnect POST, an empty final queue, and zero browser errors. All 465 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. The exact live assets returned 200; the first final-deployment window contained 34 HTTP 200 responses and no runtime error cluster. Full evidence is in `docs/IDLE_SYNC_TRAFFIC_RELEASE_2026-08-29.md`. | Verified correction; first complete 24-hour request-volume and cross-device outcome pending |
| Canonical product/company acquisition routes | Commits `b59b7fe` and `0d9167f`; final deployment `dpl_9mWErW7aui3eoV2Xp3FddCDia4hL`; production previously returned duplicate 200 responses for `/ask-crump` and `/clever-crump`, exposed the company page on AskCrump.com, and served Ask’s robots/sitemap bodies on CleverCrump.com. Permanent redirects now collapse internal slugs to the correct canonical domain and move Clever-host app, creation, legal, and deletion routes to AskCrump.com without losing campaign, signup, source, or plan queries. Clever Crump now has same-host robots and a one-URL sitemap containing only its homepage; Ask’s sitemap remains Ask-only, and its robots rule names the clean deletion path. A first deployment exposed Vercel static-file precedence; the final rule order corrected it and the followed production matrix proved direct-200 canonical roots, loop-free redirects, preserved queries, and host-correct discovery bodies. All 462 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed; the final window contained 18 HTTP 200 runtime responses with no 4xx/5xx response or error cluster. Full evidence is in `docs/CANONICAL_DOMAIN_DISCOVERY_RELEASE_2026-08-29.md`. | Verified correction; 28-day organic/search outcome boundary pending; Search Console submission not performed |
| Immutable official workspace lockup | Commit `2775c51`; deployment `dpl_CdyMyRqjeusMX9uic9RnPctrC11R`; the final small refresh nudge was traced to the visible desktop library logo remaining lazy/asynchronous and the current social-positioning bitmap reusing the legacy descriptor URL. The approved compact “An AI workspace for work that continues” artwork now has its own immutable URL, is critical and intrinsically sized in both visible top-logo positions, is normalized in place, and is distributed by cache revision `r123`. The new asset is byte-identical to the approved current artwork, with SHA-256 `32831F47A67416D9864D17C0788D354382A829AB1092C7317C6ED70674541F71`. The first signed-in production logo crop after the runtime gate cleared was pixel-identical four seconds later while the source and ready navigation state remained unchanged. All 458 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed; 84 observed release responses were 200 with no 4xx/5xx response or runtime error cluster. Full evidence is in `docs/IMMUTABLE_WORKSPACE_LOCKUP_RELEASE_2026-08-29.md`. | Verified correction; owner physical PWA refresh confirmation pending |
| Contextual acquisition-to-signup handoff | Commit `0e6ee53`; deployment `dpl_E99hQJ1PzviET4XgLKCk7SicJnM1`; the trailing-seven-day dashboard showed 121 visitors, 30 signup-intent visitors, and only two signup starts, while the internal-excluded Supabase funnel still contained zero comparable external accounts. Registration now preserves the fixed allowlisted document, presentation, résumé, video, Professional, or Enterprise promise in its headline, explanation, and primary action; paid-plan copy states the exact $20/$50 price and requires checkout review before purchase. Explicit generic Free signup clears stale specific intent. The real controller and exact deployed build proved presentation, Video + Professional, Enterprise, and stale-specific-to-Free paths without credentials or a production write. All 457 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Eleven observed release responses were 200 with no registration POST, warning/error/fatal log, or runtime error cluster. Full evidence is in `docs/CONTEXTUAL_SIGNUP_HANDOFF_RELEASE_2026-08-29.md`. | Verified delivery; first 50 additional legitimate signup-intent visitors or 14-day outcome boundary pending |
| Stable desktop official-brand handoff | Commit `af99087`; deployment `dpl_5sinMLpbfLYZLH36GPVDZ8LjXp1f`; a 1280-pixel signed-in trace isolated a remaining desktop-only 20-pixel wordmark settle to the initial 74-pixel rail becoming the final 94-pixel five-destination rail. Both layers now reserve 94 pixels from first paint and cache revision `r121` distributes the correction. The decoded 198-by-50 official wordmark stayed at x=111/y=16 from 200 ms through eight seconds while navigation became ready. All 455 tests, 45 JavaScript validations, production preflight, and native web build passed; 60 observed release responses were 200 with no warning/error/fatal log or runtime error cluster. Follow-up evidence is in `docs/HEADER_BRAND_STABILITY_CORRECTION_2026-08-29.md`. | Verified correction |
| Clear, measurable plan value | Commits `7df3503`, `84b787e`, `c889adf`, and `af99087`; final deployment `dpl_5sinMLpbfLYZLH36GPVDZ8LjXp1f`; public and signed-in surfaces now show the enforced Free, Professional, and Enterprise allowances plus the Crump Credit boundary. Supabase migration version `20260829141423` adds a service-role-only aggregate plan-view-to-checkout report. The content-free daily event accepts only its fixed name/key and one of three allowlisted sources. Signed-in production returned `accepted:settings`; the 15-minute internal-included aggregate showed one legitimate plan-center-view account and zero checkout events because checkout was deliberately not entered. Full evidence is in `docs/PLAN_VALUE_AND_CONVERSION_MEASUREMENT_RELEASE_2026-08-29.md`. | Verified delivery and measurement; legitimate external conversion outcome pending |
| Stable, current header identity | Commit `38edb79`; deployment `dpl_3o8xUdoJVUnZVw914NDcosMGTv88`; the user-observed refresh twitch was traced to three branding layers replacing the same header node after paint. The V1 header now owns one preloaded, eager, synchronously decoded 1200×296 canonical wordmark and normalizes that node in place while preserving later workspace context. The canonical visual and parent-company accessible label now use “An AI workspace for work that continues,” matching the approved Facebook cover without generative redrawing. A 390×844 production refresh trace found the decoded 170×40 logo at x=108/y=16 by 250 ms and unchanged through eight seconds while the later context child arrived without moving it. All 445 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed; the READY deployment had no alias error or production error/fatal log. Full evidence is in `docs/HEADER_BRAND_STABILITY_CORRECTION_2026-08-29.md`. | Verified correction; owner physical iPhone/PWA refresh confirmation pending |
| Measurable cold-signup exploration | Commit `a094348`; deployment `dpl_J6MZSx7Z77u9Wi2c2V52WsxPjghh`; the trailing-seven-day dashboard showed 116 visitors, 435 page views, 30 signup-intent visitors, and only two signup interactions, while the internal-excluded Supabase funnel still contained zero comparable external accounts and the artifact journey returned no rows. The restrained registration exploration link was therefore a material but unmeasured choice. It now records `RegistrationExplore` with only the existing allowlisted source, acquisition, plan, and intent categories plus `overview`, `document`, `presentation`, `resume`, or `video` destination; invalid values fail closed to overview, navigation is not delayed, and no URL, referrer URL, email, account, content, credential, or free-form value is collected. A credential-free real-controller browser fixture produced the exact `SignupIntent` then `RegistrationExplore` sequence for presentation intent without a production write. All 441 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. The exact production asset returned 200; the READY deployment had eight observed HTTP 200 responses, no warning/error/fatal log, and no runtime error cluster. Full evidence is in `docs/REGISTRATION_EXPLORATION_MEASUREMENT_RELEASE_2026-08-29.md`. | Verified measurement; first 50 additional legitimate signup-intent visitors or 14-day decision boundary pending |
| Flicker-free startup stylesheet handoff | Commit `53d5af2`; deployment `dpl_9szAKXJ5K1vR6XVwFpPMV3ibTJFG`; the owner's repeat report correctly showed that the preceding gate-timing fix had not removed every refresh defect. A higher-frequency signed-in production capture exposed a second cause: moving the live V1 stylesheet briefly detached its CSS, producing an oversized brand mark and partially styled workspace. Both runtime loaders now preserve the active stylesheet while a cached clone takes final cascade position; the visible startup mark is preloaded and eagerly decoded; cache revision 116 distributes the corrected shell. The corrected production reload was sampled 44 times with zero visible unstyled-gate frames, the mark decoded before the workspace became visible, and the intended fade completed with no busy/inert residue. A separate 32-sample signed-out local reload also recorded zero unstyled or missing-mark frames. A follow-on authenticated production refresh at a 393-by-659 iPhone-sized viewport loaded all 22 stylesheets, decoded the mark, cleared the gate/busy/inert state, preserved zero horizontal overflow, and produced no browser warning or error. All 441 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. The deployment is ready, its exact assets returned 200, the inspected window contained 48 HTTP 200 responses, and no runtime error cluster appeared. Full evidence is in `docs/STARTUP_TRANSITION_CORRECTION_2026-08-29.md`. | Verified correction; mobile-sized production refresh passed; physical Safari/PWA confirmation pending |
| Stable returning entry and contextual cold-signup exploration | Commit 54ad911; deployment dpl_3GNcGWaZz2K8DCg7v1EzGAognKi1; an owner-observed refresh glitch was isolated to the loading cover releasing before the first authenticated workspace render settled. The cover now waits until base initialization, authenticated initialization, authenticated-ready listeners, and two browser paint frames complete, while retaining the bounded five-second safety release. Cold registration also offers one restrained route back to the main product or the existing allowlisted document, presentation, résumé, or video page without changing auth, verification, attribution, pricing, analytics events, or private data. A credential-free real-controller fixture and clean local browser passed; the deployed signed-in workspace refreshed to a complete Ask surface with no busy/inert residue or browser warning/error; read-only deployed assets contained all four destinations. All 441 tests, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Production health returned 200/version 5.9.75; all six aliases are ready; the inspected deployment window contained 51 HTTP 200 responses and no warning/error/fatal log. Full evidence is in docs/ENTRY_QUALITY_RELEASE_2026-08-29.md. | Verified repair and delivery; real-device repeat and legitimate signup outcomes pending |
| Real-user performance measurement across growth surfaces | Commits `ddb4344`, `a3fc449`, `448841a`, and `cf39fa6`; deployments `dpl_8H52xj6sQDmT55HznVmcmLenmxbo`, `dpl_DtaaNbQUhvpvekMjG973eBfjuyGy`, `dpl_3KmLcZ97Pra7DpG8qY7Drw5kQ1z3`, and `dpl_8CsKxRq3wQz73hjGyNURMj6wAGMc`; Vercel Speed Insights was enabled but had no events, Real Experience Score, or Core Web Vitals sample. Explicit routes corrected seven verification events grouped as `Unknown`; a bounded follow-up produced three labeled `/` events. A subsequent audit found that `/app` accepts password-reset tokens in its query string, while both Vercel collectors contain page-event URLs. One shared fail-closed `beforeSend` boundary now removes all query strings and fragments before Web Analytics or Speed Insights transmission. Executable contracts reduce a synthetic token URL to the canonical app URL for both queues; live production loads that configuration before both collectors. Clever Crump now has first-party Web Analytics coverage as well as performance measurement. No real token or customer content was used, and no prior real-token capture is claimed. The latest complete pass covered 434 regressions, 45 JavaScript files, production preflight, native web build, and store metadata; live app and parent-company checks had no overflow or warning/error console entry. All six aliases point to the unified release, with no runtime error cluster or warning/error/fatal log after deployment. Full evidence is in `docs/REAL_USER_PERFORMANCE_MEASUREMENT_RELEASE_2026-08-29.md`. | Verified route-labeled, URL-redacted acquisition and performance instrumentation; legitimate mobile and desktop cohort pending |
| Truthful direct-signup acquisition attribution | Commit `aba3f3d`; deployment `dpl_BKbNy1DM7pyGfQ5Xohqkggkeg3Aw`; the trailing-seven-day dashboard showed 29 signup-intent visitors, including 22 with Facebook referrers, but the event-property view labeled 18 visitors as direct because direct-to-app links bypassed the landing reducer. The internal-excluded service-role snapshot returned zero comparable new accounts, so no signup or activation rate is claimed. Production cache revision 115 now applies the same privacy-minimized first-touch mapping on direct app entry: known search, social, Clever Crump, other external, same-site, absent, and malformed referrers reduce to allowlisted categories without retaining a URL, path, search term, account, credential, or content. Explicit and stored first-touch attribution remain authoritative. Executable reducer coverage, all 432 tests, 44 JavaScript validations, explicit Python compilation, production preflight, native web build, and store metadata checks passed. All six aliases point to the release; canonical health returned 200/version 5.9.75; clean and authenticated browser checks preserved the 9/4 deferred shell and 38/21 runtime-ready workspace respectively; and no runtime error cluster or warning/error/fatal log appeared. Full evidence and the unchanged decision boundary are in `docs/DIRECT_SIGNUP_ATTRIBUTION_RELEASE_2026-08-29.md`. | Verified measurement repair; first legitimate external account and 50-visitor/14-day decision boundary pending |
| Lean authentication-first app boot | Commit `68d4baa`; deployment `dpl_7zxM9j7YUhgUy3mRE9519W53RoYi`; production cache revision 114 moves 13 authenticated-workspace scripts and three workspace styles behind a server-confirmed account while keeping login, registration, recovery, and device-auth support immediate. Service-worker registration now waits on authentication only for a genuinely new signed-out browser; an existing installed PWA still registers and updates immediately. Against the clean revision-113 baseline, signed-out startup fell from 133 to 17 request lines (87.2%), 69 to 15 unique static files (78.3%), 1,269,370 to 415,736 uncompressed static bytes (67.25%), 640,684 to 69,576 JavaScript bytes (89.1%), and 381,410 to 123,605 CSS bytes (67.6%). Credential-free saved-session, login, recovery, creation-intent, runtime-idempotence, and service-worker fixtures proved the handoff without credentials or production writes. The exact production deployment retained nine scripts, four styles, focused login, zero hidden workspace marks, and zero horizontal overflow at 390-by-844 and 1,440-by-900 while keeping the workspace runtime unloaded. The existing authenticated canonical session then loaded the complete 38-script/21-style workspace, reached runtime-ready, exposed Ask, Chats, Projects, Create, Library, You, and New, and showed the mobile Ask surface with zero browser log. All 432 tests, 44 JavaScript validations, explicit Python compilation, production preflight, native web-bundle build, and store metadata checks passed. Production health returned 200/version 5.9.75, all six aliases point to the release, and the release window had no runtime error cluster or warning/error/fatal log. | Verified delivery; cold-entry interaction, activation, and field-performance outcomes pending |
| Lean first-visit PWA cache | Commit `e2efc58`; deployment `dpl_F3eUrcTHLs3DpEE7sS62L65sDDzz`; the clean-origin service-worker trace behind cache revision 112 exposed five more on-demand images in mandatory precaching. Revision 113 removes the unused 151,235-byte dark wordmark and the 22,941-byte 180px, 119,642-byte 512px, and 354,037-byte 1024px install images from first-visit requests, deferring an additional 647,855 bytes. The 25,363-byte 192px favicon still loaded normally because the live document actually references it; all install/brand files remain available at the same URLs and the default worker path caches them when a browser genuinely requests them. A fresh 133-request local trace proved none of the four deferred files or hidden workspace mark was requested, while the visible wordmark, worker, and favicon loaded. The exact production deployment then kept the registration title, focused email field, primary action, two visible wordmarks, four unfetched hidden marks, and zero horizontal overflow at 390-by-844. All 428 tests, 44 JavaScript validations, explicit Python compilation, production preflight, native web-bundle build, and store metadata checks passed. Production health returned 200/version 5.9.75, all four aliases point to the release, two observed runtime responses were 200, and the release window had no runtime error cluster or warning/error/fatal log. | Verified delivery; cold-entry transfer and activation outcomes pending |
| Lean signed-out registration entry | Commit `5ca936a`; deployment `dpl_GPWzHsSWuFoQcR2dX96RPpdAuCzL`; a fresh-origin audit proved that registration deep links were downloading the 353,713-byte workspace mark even though every instance was inside hidden terms, onboarding, or authenticated-workspace surfaces. Production cache revision 112 now keeps the two visible 157,925-byte auth wordmarks eager, gives every hidden static and runtime-rebuilt mark an intrinsic size plus deferred decoding/loading, and removes the mark from service-worker precaching so it is fetched only when a workspace surface actually needs it. Clean production-deployment checks at 390-by-844 and 1,440-by-900 kept the registration title, focused email field, primary action, and zero horizontal overflow while all four hidden mark instances remained unfetched with zero natural width. Auth/session/runtime sequencing was not changed. All 428 tests, 44 JavaScript validations, explicit Python compilation, production preflight, native web-bundle build, and store metadata checks passed. Production health returned 200/version 5.9.75, all four aliases point to the release, the deployment returned 27 observed 200 responses, and the release window had no runtime error cluster or warning/error/fatal log. | Verified delivery; cold-entry interaction outcome pending |
| Lean parent-company first load | Commit `fd38e8e`; deployment `dpl_FmF6EV2VCuP1mYfRPi1mwg2CQo2Q`; the above-the-fold Clever Crump hero now reuses the existing 353,713-byte production mark instead of downloading the 4,622,802-byte master PNG. Transparent-canvas bounds were measured before changing the rendered box from 62% to 38%, preserving the former visible footprint while removing 4,269,089 bytes, or 92.35%, from that request. Desktop, 390-by-844, and 320-by-667 browser checks proved the intended composition, current header behavior, exact production asset source and dimensions, zero horizontal overflow, and no browser warning/error. All 427 tests, 44 JavaScript validations, explicit Python compilation, production preflight, native web-bundle build, and store metadata checks passed. Production health returned 200 and the release window had no runtime error cluster or warning/error/fatal log. | Verified delivery; field performance and acquisition outcomes pending |
| Polished narrow-phone public entry | Commit `69da471`; deployment `dpl_FuwFiRL4nFxaYMwS53j77sc7HrP5`; the Ask Crump, long-label Video Studio, and Clever Crump header actions now remain single-line, overflow-free 44-pixel targets at a 320-by-667 viewport. Ask Crump preserves the complete label; Clever Crump shortens only the visible prefix below 370 pixels while keeping the destination and an accurate accessible action name. The existing 390-pixel and 1,440-pixel layouts remain unchanged. All 427 tests, 44 JavaScript validations, explicit Python compilation, production preflight, and native web-bundle build passed. Production serves 5.9.75/cache revision 110 across all four aliases, the three live narrow-width checks reported no browser warning/error, health returned 200, and the release window had no runtime error cluster or warning/error/fatal log. | Verified repair; public-entry outcome pending |
| Truthful document output proof | Commit `7d82066`; deployment `dpl_CnHRGvnptVZE7Qqc6X4M9VEk44sh`; production 5.9.75 adds a full-size 1,224-by-1,584 synthetic render from the current Word exporter to the document page before signup. The section names editable `.docx` structure, preserves `intent=document` attribution, and explicitly discloses fictional content, variable output, and the absence of customer content or a testimonial. A two-page quality render exposed and verified the correction of an orphan table-header defect before the one-page proof was accepted. The complete Word render, local desktop/phone-width layout, and production semantic/visual surface passed review. All 426 tests, 44 JavaScript validations, production/store checks, CI `33237533712`, Android `33237533699`, and iOS `33237533709` passed. Production serves 5.9.75/cache revision 109 with no release-window runtime error cluster. | Verified delivery; acquisition-to-document outcome pending |
| Truthful résumé output proof | Commit `fd95c32`; deployment `dpl_Fmwqk27Pbsph6xa5peF3ABp4gdvN`; production 5.9.74 adds a full-size 1,275-by-1,650 synthetic render from the current Word exporter to the résumé page before signup. The section names editable `.docx` structure, preserves `intent=resume` attribution, and explicitly discloses fictional content, variable output, and the absence of customer content or a testimonial. The complete one-page Word render, local desktop/phone-width layout, and production semantic/visual surface passed review. All 424 tests, 44 JavaScript validations, production/native/store checks, CI `33236210637`, Android `33236210639`, and iOS `33236210648` passed. Production serves 5.9.74/cache revision 108 with no release-window runtime error cluster or warning/error/fatal runtime log. | Verified delivery; acquisition-to-résumé outcome pending |
| Immediate, race-safe account entry | Commit `1f49a39`; deployment `dpl_3aKh1GgGrP1qN77WtvcWhr4zb2rb`; production 5.9.73 removes the deterministic dead-air window that hid both auth and app surfaces during the bounded session probe. Signup deep links expose the existing form immediately; returning visitors receive the branded loading gate with the workspace inert; a revision guard prevents late bootstrap results from overwriting an active login or registration. The delayed-session fixture proved signed-out signup, signed-out return, saved-session handoff, and active-registration race behavior without credentials or production writes. All 423 tests, 44 JavaScript validations, production/native/store checks, CI `33233033441`, Android `33233033460`, and iOS `33233033493` passed. Production serves 5.9.73/cache revision 107 with no release-window runtime error or warning/error/fatal deployment log. | Verified repair; legitimate cold-entry interaction outcome pending |
| Exact native billing identity | Commit `7543093`; deployment `dpl_5dHGpkcZ3MgmQF8FjywtnBtXLqYp`; production 5.9.72 replaces client and server package-name inference with one non-secret RevenueCat catalog and exact entitlement/product matching. Unknown active entitlements now fail fully closed as Free/inactive, credit packs require exact product IDs, and native release verification rejects malformed, duplicate, or stale identifiers. No product, price, key, account, signed build, or submission changed. All 421 tests, 44 JavaScript validations, production/native/store checks, CI `33230117812`, Android `33230117806`, and iOS `33230117811` passed. Production serves 5.9.72/cache revision 106 with no release-window runtime error or warning/error/fatal deployment log. | Verified safety release; signed-device billing proof pending |
| Conversational document delivery | Commits `c4ef9ee` and `26e41e4`; explicit format requests cannot be downgraded to clarification, and ordinary contextual follow-ups such as “make that downloadable,” “export it,” and “turn that into a file” now retain the most recent user-authored document class even when semantic routing is unavailable. Newer image/video/manuscript context and ambiguous “Save it” wording fail closed. A real `/api/chat` regression with no semantic intent proved PPTX guidance, packaging, and server-authoritative requested/packaged milestones. All 440 regressions, explicit Python compilation, 45 JavaScript validations, production preflight, native web build, and store metadata passed. Deployment `dpl_HXwQSeb1bqirPBfvC1tBpeWAZpRt` is `READY` on all six aliases; canonical health returned 200/version 5.9.75 with no browser error, runtime error cluster, or warning/error/fatal deployment log. Full evidence is in `docs/CONTEXTUAL_ARTIFACT_DELIVERY_RELEASE_2026-08-29.md`. | Verified delivery reliability; legitimate external artifact outcome pending |
| Professional presentation exports | Commit `b98d82a`; dark/light editorial rhythm, executive layouts, improved tables, native editable charts, and strict OOXML chart compatibility; full backend suite, JavaScript validation, production preflight, native build, and a ten-slide render review passed; production health returned HTTP 200 after deployment. | Verified |
| Restrained presentation visual rhythm | Commit `8610584`; deployment `dpl_5gfTeD8G3StkKRvvNhpdEufe3gtQ`; production 5.9.58 replaces repetitive equal-column output with editable statement, alternating split, asymmetric three-point, evidence, data, and purposeful dark layouts. A single table lead now stays with its native chart instead of creating a sparse duplicate slide. A nine-slide mixed deck and four-slide rhythm deck passed native PowerPoint render review with zero measured text overflow; all 384 tests, 44 JavaScript validations, production/native/store checks, CI `33192168201`, Android `33192168206`, and iOS `33192168205` passed. Production returned 5.9.58/cache revision 92 with no release-window runtime error. | Verified delivery; real-user quality outcome pending |
| Discoverable desktop conversations | Commit `cd87e3f`; deployment `dpl_HnFmTa3DKW1Vk6Knz2nRgLHuLCbE`; production 5.9.59 responds to a direct owner discovery failure by naming New, Chats, and Projects in the compact desktop rail. Chats now exposes and visually tracks its open state, preserves the remembered preference, and makes the collapsed list hidden plus inert. A credential-free browser proved both accessible states without a network write. All 387 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33195911931`, Android `33195912823`, and iOS `33195912147` passed. Production returned 5.9.59/cache revision 93 with no release-window runtime error cluster. | Verified delivery; returning-user outcome pending |
| Reliable desktop and mobile navigation | Commit `61ea00a`; deployment `dpl_AxX8py6sdMRREjqR5YXCSqtf4Puq`; production 5.9.60 corrects two owner-reproduced failures. The final desktop shell now retains a permanent Chats control and clears the stranded collapse preference once; mobile conversation options use a hydration-safe delegated handler, while Projects, Settings, and Plan & credits open before the drawer closes. A production-layer fixture and authenticated live checks proved exact conversation options, all three mobile destinations, and desktop collapse/reopen state. All 391 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33199837780`, Android `33199837741`, and iOS `33199837726` passed. Production serves 5.9.60/cache revision 94 with no release-window runtime error cluster. | Verified repair; repeated real-device use pending |
| Truthful Project continuity destination | Commit `dd7848a`; deployment `dpl_Fh7ZLzDbBNLHPVneLsmX1YQvZmWh`; production 5.9.61 replaces `Keep in a Project` with `Start a Project` or the exact named destination, then freezes that target at click time so delayed hydration cannot silently reroute the save. A local production-layer fixture proved selected, new, and delayed-loading paths with exact requests and zero browser errors; authenticated production inspection confirmed the named action without writing Project data. All 391 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33201063440`, Android `33201063452`, and iOS `33201063450` passed. Production serves 5.9.61/cache revision 95 with no release-window runtime error cluster. | Verified delivery; Project-adoption and return outcome pending |
| Truthful presentation output proof | Commit `84eddec`; deployment `dpl_J49gDvXouaQXjiWpobM4kSowkby7`; production 5.9.62 adds three 1,600-by-900 synthetic slide renders from the current PowerPoint exporter to the presentation page. The gallery is explicitly representative, makes no universal-quality claim, and contains no customer content or testimonial. Desktop and 390-pixel mobile checks proved the three-image layout, full source dimensions, zero horizontal overflow, and zero browser errors. All 393 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33202997095`, Android `33202997026`, and iOS `33202997094` passed. Production serves 5.9.62/cache revision 96 with no 30-minute runtime error cluster. | Verified delivery; acquisition-to-artifact outcome pending |
| Dedicated private Library | Commit `4fc7b40`; deployment `dpl_H8AmzuGM8t6AvR1gVrZNNmPTYSeD`; production 5.9.63 consolidates the manuscript bookshelf and saved documents/images/videos/exports/uploads into one top-level Library, removes the redundant Saved/Library workspace tab, preserves Chats as conversation history, and restores Files to attachment. Desktop and 390-pixel fixture checks proved one visible Library panel, both owner-scoped content groups, correct active state, intact Projects/Create paths, and zero browser errors; authenticated production proved the same dedicated structure without recording private content. All 394 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33204557657`, Android `33204557680`, and iOS `33204557672` passed. Production serves 5.9.63/cache revision 97 with no 30-minute runtime error cluster. | Verified delivery; reuse and return outcome pending |
| Isolated primary destinations | Commit `bdef095`; deployment `dpl_5F2JeFyYrGwGvHaLuFqw66cwWyPm`; production 5.9.64 removes the remaining Projects/Manuscripts/Video tab bar, gives Projects, Manuscripts, Video Studio, and Library distinct titles and active primary destinations, and adds an explicit Projects recovery when Manuscripts has no active Project. Files remains attachment and Chats remains history. Desktop and 390-pixel fixture checks plus authenticated production proved exactly one visible panel, zero internal tabs, correct active state, and contained mobile width without writing customer data. All 395 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33207125922`, Android `33207125976`, and iOS `33207125988` passed. Production serves 5.9.64/cache revision 98 with no 30-minute runtime error cluster or warning/error/fatal deployment log. | Verified delivery; discovery, reuse, and return outcomes pending |
| Clean cross-device conversation startup | Commit `1d7e908`; deployment `dpl_DU1j6CsCCnHFGBcjtH4quoYjAych`; production 5.9.65 keeps an untouched fresh canvas ephemeral and materializes a conversation only when a real send begins. Local cache, server merge, and outbound sync suppress legacy default-titled empty rows without deleting server/customer data. A production-layer fixture proved a clean device pulling one real chat plus two legacy blanks retained exactly one durable row and pushed zero blanks, then created exactly one conversation on first send. The same release removes programmatic-focus inflation from `SignupStarted`. All 399 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33210710079`, Android `33210710087`, and iOS `33210710074` passed. Production serves 5.9.65/cache revision 99 with no one-hour runtime error cluster or warning/error/fatal deployment log. | Verified repair; repeated real-device history cleanliness pending |
| Distinct company identity and contained desktop canvas | Feature commit `05418a5`; routing stabilization `8b960d1`; final deployment `dpl_Em4W8i3M1jjFPexcv33hkXBcNo3c`; production 5.9.66 gives Clever Crump a dedicated parent-company landing at both clean root domains while preserving the Ask Crump product root. It also cancels a legacy 292-pixel offset that was double-counting the open Chats library and pushing the signed-in workspace beyond a 1,280-pixel viewport. Desktop and 375-pixel fixtures, production screenshots, and semantic snapshots proved distinct brand surfaces, a centered contained app canvas, and zero measured horizontal overflow. All 402 tests, lint/compile, 44 JavaScript validations, production/native/store checks, feature CI `33213150773`, Android `33213150645`, iOS `33213150810`, and routing CI `33213752591` passed. Production health serves 5.9.66 and the one-hour warning/error/fatal scan was empty. | Verified delivery; brand handoff and first-use outcomes pending |
| Single-responsibility Chats and You destinations | Commit `127ed27`; deployment `dpl_97Rmrz5JdkUZMaPhtWzuzGPcRcQV`; production 5.9.67 removes Projects, Settings, Plan, legal, and account-sync duplication from the Chats panel. Chats now contains conversation history only; You owns Profile, Behavior, Plan & credits, Account, and About, with the live balance and unchanged billing center reachable from the dedicated Plan section. Save changes appears only on editable settings sections. Desktop and 390-pixel production-layer fixtures plus authenticated production snapshots proved the exact hierarchy and billing handoff without a checkout or customer write. All 405 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33215392537`, Android `33215392543`, and iOS `33215392555` passed. Production serves 5.9.67/cache revision 101 and the release-window warning/error/fatal scan was empty. | Verified delivery; navigation, Plan discovery, and checkout-entry outcomes pending |
| Single-click destination transitions | Commit `3a93531`; deployment `dpl_AVYBR9BWompwdiMc7cGvx8e2HNeR`; production 5.9.68 prevents the Projects, Create, Library, and You backdrops from covering the persistent desktop rail or mobile bottom navigation, and bounds mobile sheets above the bottom safe area. Production-layer fixtures proved Library → Create → Projects and You → Projects in one click per transition. An authenticated no-write production browser proved Projects → Create → Library → You → Ask with the correct active state after every single click. All 406 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33217042720`, Android `33217042725`, and iOS `33217042738` passed. Production serves 5.9.68/cache revision 102 and the release-window warning/error/fatal scan was empty. | Verified repair; navigation and mobile task-completion outcomes pending |
| Stable workspace entry and reorganized guide | Commit `ee5428c`; deployment `dpl_8kuUjepL31DL8s3ecVQdxB2HY7jK`; production 5.9.69 replaces the brief obsolete-rail flash with a bounded branded workspace handoff, then releases to the stable Ask, Chats, Projects, Create, Library, and You navigation. The replayable guide now teaches exactly Ask with Chats as history, Projects, Create, Library, and You, with a matching active destination on all five steps. A production-code fixture and authenticated live replay proved every step; an exact first-visible-frame check proved the handoff appears before the final runtimes settle and disappears after they are ready. All 408 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33218241231`, Android `33218241102`, and iOS `33218241194` passed. Production serves 5.9.69/cache revision 103 and the one-hour warning/error/fatal scan was empty. | Verified repair; first-session confidence, guide completion, and destination-discovery outcomes pending |
| Recoverable database reads | Commit `abfc6ce`; deployment `dpl_8MwNZ78Rk5FFFgqEi1g2kYjQYVrY`; production 5.9.70 responds to 28 observed `/api` database 5xx failures with bounded retry of transient idempotent reads only. Writes and RPC calls remain single-attempt; retry diagnostics are categorical and exclude tables, filters, identifiers, provider text, and private data. Transport/status exhaustion, write-safety, non-transient, and response-privacy regressions passed with all 413 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI `33223161380`, Android `33223161346`, and iOS `33223161377`. Production serves 5.9.70/cache revision 104 with no initial release-window runtime error or warning/error/fatal log. | Verified delivery; organic recovery and recurrence outcome pending |
| Value-aware cold signup entry | Commit `60f2c7d`; deployment `dpl_5ovTWjwVjq7iUSf9u92gXqR8Lr5w`; production 5.9.71 responds to seven last-24-hour `SignupIntent` visitors—six socially referred and none reaching `SignupStarted`—without treating that small sample as a conversion rate. A credential-free production-markup audit reproduced the deterministic mobile gap: cold visitors saw the form and free/no-card reassurance while the full product promise was hidden with the desktop brand panel. The signup introduction now explains asking, creating, and continuing work; verification assurance is more readable. Auth, password, verification, pricing, attribution, and funnel semantics are unchanged. Desktop, 390-by-844, and 390-by-667 checks kept email focused and the primary action visible with zero browser errors. All 414 tests, 44 JavaScript validations, production/native/store checks, CI `33224902387`, Android `33224902413`, and iOS `33224902385` passed. Production serves 5.9.71/cache revision 105; the initial deployment window had two 200 responses and no runtime error or warning/error/fatal log. | Verified delivery; cold-entry interaction outcome pending |
| Private artifact journey telemetry | Commit `f497ab0` established server-authoritative, content-free request, packaging, failure, and first-download events. Supabase migration `20260830192948 artifact_journey_cohort_boundary` now keeps deleted, internal, preview, and development accounts out of production totals by default; the live aggregate remains service-role-only and returned no external production rows after migration. Full validation and Supabase security/performance advisors were green with informational findings only. | Verified delivery; first external artifact journey pending |
| Crump Code private foundation | Commit `018b46c`; deployment `dpl_GjeFNqmhK32QeyQyXKLrDoePxViu`; production 5.9.35 adds a Project-attached review workspace for repository/revision/mode/objective/cost confirmation, task status, explicit approvals, verification, history, cancellation, and patch download. Preparation does not run or charge; the client and server both require explicit run confirmation, and cancellation is checked before each next model/tool step. The Create entry remains hidden unless the server reports configured plus entitled. All 313 tests, lint, 42 JavaScript validations, production/native/store checks, CI run `33134659887`, Android run `33134659984`, and iOS run `33134659934` passed. Production health returned 5.9.35; assets returned 200; the inspected deployment had no runtime error cluster, 5xx, severe log, or `/api/code` request. The feature flag remains off pending the live sandbox/OIDC test, expiry exercise, and benchmark. | Staged, disabled |
| Clear signup password readiness | Commit `40bbc28`; deployment `dpl_5kDcdWj7KpWHbq9kXrjDJacQjESV`; production 5.9.36 replaces a late static password hint with three live, visible rule states plus a polite screen-reader status and post-review invalid state. The unchanged policy remains ten to 256 characters with a letter and number; auth, verification, pricing, and analytics semantics did not change. All 314 tests, lint, 42 JavaScript validations, production/native/store checks, CI run `33135663864`, Android run `33135663895`, and iOS run `33135663885` passed. Production health returned 5.9.36; the app and changed assets returned 200; the inspected release had no runtime error cluster, severe log, or 5xx. Local desktop/short-phone states had no overflow and no production event or account creation. Signup lift remains unproven. | Verified delivery; outcome pending |
| Durable registration verification handoff | Commit `ebd1454`; deployment `dpl_EVrjwoQRXALKP1UvqvSnpZYsFsnj`; production 5.9.37 replaces a 1.8-second success message and generic-login redirect with a persistent, focused inbox-confirmation state, prefilled email, explicit verified/sign-in action, resend action, and durable success/failure feedback. The account-created/email-delivery-failure branch uses the same recovery surface, and content-free account creation now records only sent/failed verification delivery. Password, registration, verification, authentication, pricing, and entitlement policy remain unchanged. All 315 tests, lint, 42 JavaScript validations, production/native/store checks, CI run `33136496183`, Android run `33136496185`, and iOS run `33136496204` passed. Production health returned 5.9.37; the app and changed assets returned 200; the inspected release had no runtime error cluster, severe log, 5xx, or registration request. No account or synthetic funnel event was created. | Verified delivery; outcome pending |
| Optional profile activation entry | Commit `81b39be`; deployment `dpl_G7oecbc71CPp6913TWvDoacTxsSv`; production 5.9.38 removes the mandatory display-name commitment between verified, terms-accepted users and the first workspace. Terms remain required and server-saved; name setup is a dismissible, account-scoped launchpad prompt, and `OnboardingCompleted` remains server-authoritative. Settings no longer reports a rejected profile update as saved. All 320 tests, lint, 42 JavaScript validations, production/native/store checks, CI run `33137238298`, Android run `33137238297`, and iOS run `33137238319` passed. Production health returned 5.9.38; the app and changed assets returned 200; the inspected deployment had no runtime error cluster, severe log, 5xx, signup, profile, terms, account, or activation request. No synthetic account or event was created. | Verified delivery; outcome pending |
| Reliable first workspace choice | Commit `e8fb9f0`; deployment `dpl_HE8v2SbtqeuayEJajMYiTrLt3Q1p`; production 5.9.39 replaces the launchpad's fixed 120-millisecond Projects/Video readiness guess with the runtime completion event. A delayed-load browser reproduction recorded starter intent but opened nothing under the old path; the corrected path waits visibly, opens the queued workspace exactly once, restores the card, reports a real asset failure, and lets the latest choice win. All 324 tests, lint, 42 JavaScript validations, production/native/store checks, CI run `33137897554`, Android run `33137897556`, and iOS run `33137897614` passed. Production health returned 5.9.39; the live readiness/cache assets returned 200; the release had no runtime error cluster, non-informational log, or 5xx. No production click, account, or synthetic event was created. | Verified delivery; outcome pending |
| Truthful first-prompt handoff | Commit `3ab5acb`; deployment `dpl_CT2aQtDDAwNLAEc2MzDwoWkvCaeW`; production 5.9.40 corrects a browser-reproduced composer handoff where Research/Image erased an existing draft and programmatic text did not update the active composer state. Research, Image, and Code now prefix the draft once, emit the real input event, preserve focus/caret, and stop an exact bare scaffold before usage checks or chat mutation. File and starter-intent contracts remain unchanged. All 329 tests, lint, 42 JavaScript validations, production/native/store checks, CI run `33138434467`, Android run `33138434500`, and iOS run `33138434478` passed. Production health returned 5.9.40; live composer/cache assets returned 200; the release had no runtime error cluster, non-informational log, or 5xx. No production prompt, account, usage check, or synthetic event was created. | Verified delivery; outcome pending |
| Reliable authenticated entry | Commit `ee3862d`; deployment `dpl_7sBD8Y3e8oyW696ec7HpHBLNLMVU`; production 5.9.41 removes the secondary full-state sync from the authenticated-entry critical path. A credential-free browser fixture proved that a never-settling sync left a completed login on a permanently disabled `Signing in…` button and a restored session on a blank screen. Both corrected paths open the account-scoped shell immediately while the existing server-authoritative synchronizer continues in the background. Credentials, verification, session rotation, cookies, ownership, pricing, entitlements, analytics, Supabase schema, and RLS remain unchanged. All 332 tests, lint, 42 JavaScript validations, production/native/store checks, CI run `33139229180`, Android run `33139229175`, and iOS run `33139229205` passed. Production health returned 5.9.41; the live shell/controller/cache assets returned 200; the release had no runtime error cluster, warning/error/fatal log, or 5xx. The fixture made no production write; owner credential-entry recheck remains pending. | Verified delivery; human proof pending |
| Recoverable continuing-work sync | Commit `76455e5`; deployment `dpl_G77wN9y7d7T1ftgWch1kw8AU63zQ`; production 5.9.42 bounds sync requests through body parsing and preserves the account-scoped pending queue on timeout/network failure. A credential-free browser fixture proved the old latest-result Project action remained disabled forever; the corrected path stopped the stalled request, enabled retry, and retained exactly one queued save. Project ownership, merge/revision rules, auth, pricing, entitlements, analytics, Supabase, and payments remain unchanged. All 336 tests, lint, 42 JavaScript validations, production/native/store checks, CI run `33140100110`, Android run `33140100029`, and iOS run `33140100058` passed. Production health returned 5.9.42; the live versioned/network-first sync asset and cache revision returned 200; the release had no runtime error cluster or warning/error/fatal log. No production login, chat, Project, account, payment, or synthetic event was created. | Verified delivery; retention outcome pending |
| Fallback first-message preflight | Commit `6111540`; deployment `dpl_52eNo3CQUC3JFcooDeBsbpgx7Z4q`; production 5.9.43 bounded the early `app.js` usage preflight and preserved the draft on failure. A subsequent steady-state audit found that post-load `crump-5.0.js` replaced Send with an unbounded primary path, so the original fixture proved fallback behavior only. Its 340 tests and hosted gates remain valid for that scope; release 5.9.44 supersedes the incomplete runtime boundary. | Partial delivery; superseded |
| Recoverable first reply | Commit `4804fc4`; deployment `dpl_HgAo8qwFh1gzroqUE47SrDFqxTnf`; production 5.9.44 applies one bounded transport to both fallback and primary runtimes, covering usage, acknowledgement, reply, and response parsing. A real-primary-runtime fixture proved the old reply stalled forever and ignored a second Send; the corrected path aborted locally, reconciled the existing owner-scoped idempotent job, rendered its persisted answer, and accepted a second message. A separate acknowledgement stall exposed visible retry and completed safely. The authenticated no-store status route filters user plus message ID; schema/RLS, usage, credits, providers, pricing, entitlements, analytics, and payments remain unchanged. All 347 tests, lint, 43 JavaScript validations, production/native/store checks, CI run `33141840340`, Android run `33141840370`, and iOS run `33141840430` passed. Production health returned 5.9.44; live changed assets/cache returned 200; the one-hour scan had no runtime error cluster or warning/error/fatal log. No production login, message, generation, Project, account, payment, or synthetic event was created. | Verified delivery; activation outcome pending |
| Recoverable authentication requests | Commit `0012a30`; deployment `dpl_3QsniFHTrMSACqNWPf7DzNqMckJ2`; production 5.9.45 applies one bounded transport through response parsing to registration, verification-email resend, password recovery/reset, terms acceptance, profile save, session checks, login, logout, and native push-registration cleanup. A registration-stall fixture proved the old permanently disabled `Creating account…` state; the corrected path restores the action with truthful uncertain-outcome guidance. A login-response-stall fixture proved the web client can reconcile a session issued before the response connection stalls. Auth policy, cookies, schema/RLS, pricing, entitlements, analytics, and payments remain unchanged. All 352 tests, lint, 44 JavaScript validations, production/native/store checks, CI run `33142697258`, Android run `33142697156`, and iOS run `33142697157` passed. Production health returned 5.9.45; live assets/cache returned 200; the one-hour scan had no runtime error cluster or warning/error/fatal log. No production login, account, event, message, Project, or payment was created. | Verified delivery; human proof pending |
| Complete signup milestone delivery | Commit `6c3e546`; deployment `dpl_9Mn5E1AtiiNL1FKN345Ag6gF5LkL`; production 5.9.46 closes a deterministic measurement gap in which valid password-manager/autofill values could reach `SignupSubmitted` without emitting `SignupCredentialsReady`. Shared one-time helpers now guarantee the ordered `SignupStarted` → `SignupCredentialsReady` → `SignupSubmitted` sequence on valid typed and autofilled submissions. The payload remains content-free and excludes email/password values; registration, verification, auth, schema/RLS, pricing, entitlements, server analytics, and payments are unchanged. A real-controller loopback fixture proved the pre-fix omission and both corrected paths exactly once. All 353 tests, lint, 44 JavaScript validations, production/native/store checks, CI run `33143365291`, Android run `33143365303`, and iOS run `33143365301` passed. Production health returned 5.9.46; live controller/cache assets returned 200; the initial scan had no runtime error cluster or warning/error/fatal log. No production signup, account, event, login, message, Project, or payment was created. | Verified measurement; conversion outcome pending |
| Recoverable verification-link return | Commit `a3ae2de`; deployment `dpl_EBxtmgbDcy7y7yeEhijKBvNHMbU8`; production 5.9.47 turns an invalid, expired, already-used, or scanner-consumed verification-link result into an actionable recovery state. The signed-out screen focuses the email field, exposes the existing resend control, and truthfully offers sign-in when verification may already have completed; its generic resend result preserves account-state privacy. Token lifetime, single-use semantics, auth policy, schema/RLS, pricing, entitlements, analytics, and payments remain unchanged. A real-controller loopback fixture proved the old dead end and the corrected recovery path without a production account or token. All 354 tests, lint, 44 JavaScript validations, production/native/store checks, CI run `33143924537`, Android run `33143924544`, and iOS run `33143924530` passed. Production health returned 5.9.47; live app/controller/cache assets returned 200; the initial scan had no runtime error cluster or warning/error/fatal log. | Verified delivery; activation outcome pending |
| Durable password-reset handoff | Commit `7ab3b1b`; deployment `dpl_8srpxeQqjCoFEjF3tPYZRhYvrvnQ`; production 5.9.48 sends a successful password reset directly to sign-in with a persistent confirmation and focused email field instead of flashing a result for 1.8 seconds and leaving a blank form. The reset token is removed from the URL and deleted from form state. A real-controller loopback fixture proved a single request, durable result, clean URL, and keyboard-ready next step. All 355 tests, lint, 44 JavaScript validations, production/native/store checks, CI run `33153793086`, Android run `33153793090`, and iOS run `33153793073` passed. | Verified delivery; recovery outcome pending |
| Durable mobile sign-in and PWA wake-up | Commits `06c30a1` and `3b20cf4`; production 5.9.49 expands successful mobile web-session confirmation from roughly 275 milliseconds to a bounded 2.65-second window and checks for PWA updates on load, wake, page restore, and connectivity return. Idle signed-out pages adopt a new controller automatically; entered credentials and signed-in work receive a controlled reload prompt. A real-runtime fixture reproduced the three-check failure and proved one workspace start on the fourth check. Identity-free Ask Crump auth outcomes remain observable. All 359 tests, lint, 44 JavaScript validations, production/native/store checks, CI run `33155450505`, Android run `33155450518`, and iOS run `33155450469` passed. A later production-wide review found serverless host reconfiguration could restore dependency-level URL records; 5.9.54 supersedes that incomplete import-time suppression while preserving this sign-in/PWA repair. | Verified repair; owner credential recheck pending |
| Authenticated workspace startup boundary | Commit `ea74f83`; deployment `dpl_71UWy9RoEESp5WBGKVoT2XMvZAUE`; production 5.9.50 defers Projects/Video, Crump Code, Library, and credits hydration until the server-confirmed account reaches the workspace. A full-shell browser run changed signed-out startup from five expected 401s to one successful session check and zero protected calls, while the matched authenticated run loaded every protected surface without delay or script error. All 362 tests, lint, 44 JavaScript validations, production/native/store checks, CI run `33156887623`, Android run `33156887563`, and iOS run `33156887586` passed. The fresh production browser served `r84`, made one 200 session request, and produced no failed response, script error, or error/fatal deployment log. | Verified delivery; activation outcome pending |
| Single-owner startup and reconnect sync | Commit `f17b3f6`; deployment `dpl_EoWF3UipaBDMcCaqyct8dJssdMk5`; production 5.9.51 removes the starter conversation's delayed blind push and makes the synchronization layer the sole data-sync owner for browser reconnection while presence retains its status and announcement role. Restored-session and fresh-login browsers each produced one ordered pull/push/confirmation-pull sequence; a stalled pull produced no competing push; offline startup produced no sync traffic before exactly one reconnect sequence. All 364 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI run `33159045825`, Android run `33159045783`, and iOS run `33159045809` passed. Production served `r85` with one 200 signed-out session check, zero protected startup calls, no console error, no runtime error cluster, and no error/fatal deployment log. | Verified delivery; owner credential recheck pending |
| Recoverable private Project handoff | Commit `bde31da`; deployment `dpl_8mYEDxe4uVoEEUCy5qKt8UjMDBrx`; production 5.9.52 bounds the Project create/attach response behind `Keep in a Project`, restores the disabled action with truthful retry guidance, and confirms a completed save without waiting for the secondary list refresh. An uncertain create retry reuses the newest active owned Project already holding that conversation, including at the plan limit, and the successful button now exposes an accurate accessible action name. A credential-free browser proved the old indefinite disabled state, two corrected timeout/retry cycles, and the complete success response with zero browser errors. All 367 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI run `33162073713`, Android run `33162073773`, and iOS run `33162073775` passed. Production serves 5.9.52/r86 with the changed assets, no runtime error cluster, and no warning/error/fatal deployment log. | Verified delivery; retention outcome pending |
| Recoverable Project return reads | Commit `23e6f9e`; deployment `dpl_3VVnB261rupDFtaDQeENRrh3dc3K`; production 5.9.53 bounds the Project list, saved-conversation list, Project-note read, and their response-body parsing. Each failed surface exposes an accurately named Retry action without discarding the active Project or hiding another successful surface, and stale Project-note results cannot overwrite a newly selected Project. A credential-free real-runtime fixture proved all four stall modes, a reusable retry, and the successful return to the exact saved conversation with zero browser errors. All 368 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI run `33168768095`, Android run `33168768090`, and iOS run `33168768141` passed. Production serves 5.9.53/r87 with the changed runtime, no runtime error cluster, and no error/fatal deployment log. | Verified delivery; retention outcome pending |
| Private upstream observability boundary | Commit `44b0efa`; deployment `dpl_GSm6EFkPN6EpKCqrVV77WKTbQ8tt`; production 5.9.54 blocks every `httpx`/`httpcore` record at configured handlers and reapplies that boundary before each request, closing a serverless host-reset gap that left full Supabase filter URLs with session hashes and internal row IDs in runtime logs. Database failures retain categorical status/detail type without raw upstream detail. Tests proved host reconfiguration, direct transport handlers, retained Ask Crump auth outcomes, and excluded database sentinels. A non-writing fake-cookie production probe forced a real Supabase lookup: the sentinel hash and `HTTP Request:` were absent while the identity-free auth outcome remained. All 371 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI run `33172015491`, Android run `33172015490`, and iOS run `33172015531` passed. Production serves 5.9.54/r88 with no runtime error cluster or warning/error/fatal deployment log. | Verified privacy containment |
| Clear authentication entry handoffs | Commit `ed88c44`; deployment `dpl_B1jKEcq7mgoU2gacvvUicBBuUXfQ`; production 5.9.55 centralizes sign-in, registration, recovery, and reset transitions and moves focus to each view's first field instead of leaving it on a hidden link or the page body. Native validation blocked before the network now leaves a persistent announced message, and content-free events distinguish validation, submission, completion, and request failure. A real-controller fixture proved every transition, both direct-link entries, and empty-login validation with zero browser errors. The owner completed a real phone/PWA sign-out and sign-in and confirmed the update prompt. All 373 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI run `33179824223`, Android run `33179824154`, and iOS run `33179824243` passed. Production serves 5.9.55/r89; five database-backed session probes returned 200 and the exact deployment has no warning/error/fatal log. | Verified delivery and owner sign-in proof; conversion outcome pending |
| Qualified creation-intent continuation | Commit `5ceb57a`; deployment `dpl_ENikqcrY6BYhZHvzoDU5VGtgHnFc`; production 5.9.56 preserves only the allowlisted document, presentation, résumé, or video category through sign-in, registration/verification return, and an existing session, then opens the exact non-generating workspace promised by the public capability page. A real-controller browser exercised all four destinations, invalid-intent rejection, and a signed-out-to-authenticated résumé return; every path cleared once with zero browser errors. All 378 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI run `33185622078`, Android run `33185622086`, and iOS run `33185622138` passed. Production serves 5.9.56/r90; all four live phone-size pages carried intent plus acquisition with no overflow or console issue. Intermittent pre-existing background sync transport 503s preserved the local queue and were followed by 200; Supabase remained `ACTIVE_HEALTHY`. | Verified delivery; activation outcome pending |
| Résumé purpose through artifact delivery | Commit `de0440f`; deployment `dpl_6FnNfyGKHFKW4osPxEvLVwDajDDs`; production 5.9.57 preserves the exact allowlisted résumé purpose from the public handoff or in-app `RÉSUMÉ · CV` choice through send, sync, retry, server guidance, and DOCX/PDF packaging. The pre-correction fact-only brief resolved to a generic business document; corrected guidance and a real packaging test proved the ATS/fact-grounded résumé profile without requiring the user to repeat “résumé.” Arbitrary purpose values are discarded. All 382 tests, lint/compile, 44 JavaScript validations, production/native/store checks, CI run `33189736839`, Android run `33189736730`, and iOS run `33189736888` passed. Production serves 5.9.57/r91; the exact deployment returned only 200 in observed requests and had no release-window runtime error group. | Verified delivery; artifact outcome pending |
| Crump Voice private foundation | Explicit signed-in playback route, Professional entitlement, rate/character/audio limits, provider-failure refund, server-held ElevenLabs key, non-cacheable ephemeral MP3 response, and device-speech fallback are implemented. Public feature flag remains off pending approved disclosure, credentials/voice rights, and smoke tests. | Staged, disabled |
| Private conversation-to-Project continuity | Commit `e99fc1f`; production 5.9.22 puts `Keep in a Project` directly on the latest result, reducing durable-work preservation from two commitments to one. The existing server route synchronizes and ownership-checks the chat, attaches idempotently to the selected/new Project, and records only a content-free Project milestone. All 285 tests, backend lint/compile checks, 40 JavaScript validations, production preflight, and native web-bundle build passed. Live health and version checks returned HTTP 200, the deployed client contained the direct action, and the deployment-scoped error/fatal scan was empty. | Verified |
| Comparable growth-cohort boundary | Supabase migration `product_growth_measurement_boundary`; live first-event evidence fixes the lower bound at `2026-08-23 09:10:55.602863+00`; the 30-day report now returns 18 metrics and zero comparable external accounts instead of misclassifying three historical accounts. The function remains security invoker, `anon`/`authenticated` execution is denied, `service_role` execution succeeds, and post-change advisors reported no errors or warnings. | Verified |
| Truthful organic discovery | Commit `150ced2`; deployment `dpl_HUbcyLdLFdh7SVpqF3S99XL3caMo`; production 5.9.23 adds unique, crawlable presentation and document workflow pages, homepage/cross-page links, canonical metadata, valid JSON-LD, and a four-URL sitemap. Known search referrers collapse to `organic` without retaining the referrer URL or query, and internal CTA placements cannot overwrite acquisition. All 290 backend tests, 40 JavaScript validations, production/native bundle checks, CI run `33116981568`, Android run `33116981449`, iOS run `33116981462`, clean-URL HTTP checks, desktop/mobile browser checks, and the deployment-scoped error/fatal scan passed. | Verified |
| Truthful referral delivery | Commit `a7f3482`; deployment `dpl_2zxpBU85E3uJkygbmmjquhq4fVQJ`; production 5.9.24 keeps the post-useful-result invitation content-free and carries only aggregate `referral` acquisition plus `response-share` placement into registration. The registration server preserves `referral` on `AccountCreated`. Denied clipboard access can no longer display a false success or record `ResponseShared`; an executable browser-script contract proves failed copy records zero events while a verified fallback records exactly one. All 291 backend tests, 40 JavaScript validations, production/native/store checks, CI run `33119777886`, Android run `33119777893`, iOS run `33119777888`, live route/version checks, and the deployment-scoped error/fatal scan passed. A legitimate referred account and activated outcome have not yet been observed. | Verified delivery; outcome pending |
| Measurable social previews | Commit `6d2c24f`; deployment `dpl_CZih5NeHk8JjDp1tukrLZPCXhioD`; production 5.9.25 gives the home, presentation, and document pages distinct 1,200-by-630 social cards composed from the canonical mark, with large-card metadata and truthful page-specific copy. The generator is deterministic on the verified release machine; automated tests validate PNG format, dimensions, color mode, and per-page references. All 292 backend tests, 40 JavaScript validations, production/native/store checks, CI run `33123220073`, Android run `33123220055`, iOS run `33123220046`, six live route/asset checks, and the deployment-scoped warning/error/fatal/5xx scan passed. Socially attributed signup outcome remains unproven. | Verified delivery; outcome pending |
| Direct canonical native/payment host | Source correction shipped in 5.9.26. With owner approval, both live Stripe destinations were then changed to their direct `https://www.askcrump.com/...` handlers without rotating secrets or widening their permanent event allowlists. A signed subscription replay returned 200. The first signed credits replay exposed a deployed plural environment-key alias; commit `4dfed9b` added a backward-compatible, precedence-tested alias without exposing or rotating the secret. Deployment `dpl_H5Dn15BVY5rzh5G6azq36eKiTXb3` is `READY` on production 5.9.27, a final signed credits replay returned 200, and the temporary harmless test event was removed. All 295 backend tests, 40 JavaScript validations, production/native/store checks, CI run `33126121600`, Android run `33126121646`, and iOS run `33126121595` passed. Production health returned 5.9.27, the payment routes had no runtime error cluster, and the deployment log breakdown contained only 200/302 responses. | Verified end to end |
| Named recent-work continuation | Commit `6161778`; deployment `dpl_EJmVH3eLTbfQdPzLCpyLZ6H22RUj`; production 5.9.28 replaces the generic return card with the actual local conversation name and a clear continuation cue. Names are whitespace-normalized, length-bounded, rendered with `textContent`, visually ellipsized, and used only in the signed-in interface; `RecentWorkResumed` remains free of chat IDs, titles, and content. All 295 backend tests, lint, 40 JavaScript validations, production/native/store checks, CI run `33126950108`, Android run `33126950133`, and iOS run `33126950091` passed. Production health returned 5.9.28, desktop/mobile browser checks passed, the card opened the intended conversation, and the deployment had no runtime error cluster or warning/error/fatal/5xx response. The comparable external cohort remains zero, so no retention lift is claimed. | Verified delivery; outcome pending |
| Reliable web-session handoff | Commit `38f7d11`; deployment `dpl_4H1xjuSyrC9dBxg5WWZ95jkfrox8`; production 5.9.29 repairs a user-observed false login failure. Runtime evidence showed successful login/session writes followed by immediate unauthenticated confirmation probes. The server now checks a bounded set of same-name cookie candidates while preserving bearer precedence, canonical login retires the legacy parent-domain cookie, logout clears both scopes, and the client rotates once before bounded confirmation probes. The auth asset is release-versioned and network-first. All 298 tests, lint, 40 JavaScript validations, production/native/store checks, CI run `33128276341`, Android run `33128276312`, and iOS run `33128276343` passed. Production health returned 5.9.29, an authenticated browser opened the workspace, and the deployment had only 200 responses with no runtime error cluster or warning/error/fatal logs in the inspected window. | Verified repair; owner credential-entry recheck pending |
| Five-destination workspace navigation | Commit `86dfb2c`; deployment `dpl_8q5SK1mLXcqcExhLBvH9wgHPeTbT`; production 5.9.30 organizes the signed-in product around Ask, Projects, Create, Library, and You. It reuses every existing owner-scoped data/API/entitlement surface, adds a non-generating Create chooser, keeps Research inside Ask, and provides a device-local legacy rollback switch. All 303 tests, lint/compile, 41 JavaScript validations, production/native/store checks, CI run `33129397532`, Android run `33129397531`, and iOS run `33129397539` passed. Production health and six release assets returned 200; authenticated desktop/mobile checks passed; the inspected deployment had 23 successful 200 responses with no 5xx, warning/error/fatal log, or runtime error cluster. | Verified first slice; usability outcome pending |
| Resumable Project conversations | Commit `e67ff3b`; deployment `dpl_3FzSEnwGFXSTxh73AcdcXUD28U3t`; production 5.9.31 closes the gap between keeping work and finding it again. Owned Projects now show content-free conversation metadata and a Continue action that syncs a missing cross-device conversation before opening it. The authenticated endpoint requires both Project and conversation ownership, excludes deleted chats, and never returns messages or files. All 306 tests, lint, 41 JavaScript validations, production/native/store checks, CI run `33130217575`, Android run `33130217571`, and iOS run `33130217560` passed. Production health and release assets returned 200; the unauthenticated route returned the expected 401; an authenticated desktop/mobile audit rendered two real linked conversations without generating a synthetic resume event; the inspected deployment had no runtime error cluster or non-informational log level. | Verified delivery; retention outcome pending |
| Expanded high-intent organic discovery | Commit `afd5473`; deployment `dpl_3VQGjdTVUeDNNzRHqRa1UprHHZ2e`; production 5.9.32 adds focused, crawlable AI résumé-builder and AI video-generator pages around capabilities already verified in production. Résumé claims are fact-grounded and reject invented experience; video copy exposes credit use, compatibility, and variable output. The homepage and existing use-case pages now form a four-capability internal-link graph, the sitemap contains six canonical URLs, and both new pages have unique JSON-LD and deterministic 1,200-by-630 social cards. All 306 tests, lint, 41 JavaScript validations, production/native/store checks, CI run `33131314907`, Android run `33131314974`, and iOS run `33131314943` passed. Production health returned 5.9.32; both clean URLs, both cards, and the sitemap returned 200; desktop/phone layout checks found no horizontal overflow; the inspected deployment reported no runtime error cluster and only 200 responses. | Verified delivery; acquisition outcome pending |
| Accessible public first visit | Commit `8d03ce7`; deployment `dpl_9sMBVqXWhqSS3QgRkXYKr1G3b62o`; production 5.9.33 raises muted marketing text to WCAG AA contrast while preserving the black/charcoal/gold system. Mobile Lighthouse moved the homepage and résumé page from accessibility 95 with 13 and four contrast failures respectively to accessibility 100 with zero failures; both production runs also scored 100 for performance, best practices, and SEO. A deterministic relative-luminance test covers ten selector/background pairs. All 307 tests, lint, 41 JavaScript validations, production/native/store checks, CI run `33132384656`, Android run `33132384622`, and iOS run `33132384717` passed. Production health returned 5.9.33; mobile browser checks found no overflow or console warning/error; the inspected deployment reported no runtime error cluster and only 200 responses. | Verified delivery; acquisition outcome pending |
| Accessible workspace zoom | Commit `7f6013b`; deployment `dpl_4SgvrggyDSKbr5jJuEzipjhdo4yF`; production 5.9.34 removes the app-wide maximum-scale, user-scaling, Safari gesture, and two-finger touch blockers. The shell retains no-drift constraints and 16-pixel mobile editor safeguards while explicitly allowing vertical pan and pinch zoom. Registration moved from a local Lighthouse accessibility score of 93 with a failed meta-viewport audit to 100; local signed-out and production registration states also scored 100 with zero contrast failures. All 307 tests, lint, 41 JavaScript validations, production/native/store checks, CI run `33133179838`, Android run `33133179924`, and iOS run `33133179768` passed. Production health returned 5.9.34; the short-phone primary action stayed visible with no overflow or console issue; the inspected deployment reported no runtime error cluster, warning/error/fatal log, or 5xx response. | Verified web/PWA delivery; signed-device zoom check pending |

### Current production reliability checkpoint

The 2026-08-29 directional acquisition checkpoint covered the preceding seven days in Vercel Web
Analytics: 108 visitors, 382 pageviews, and a 58% bounce rate. Facebook and mobile Facebook
accounted for 28 referred visitors; content-free funnel events showed 28 visitors reaching a
signup-intent action and two reaching an actual signup-field interaction. These anonymous,
small-sample totals may include internal or automated traffic and remain below the precommitted
14-day and 50-social-visitor decision boundary. Authentication was therefore left unchanged.
The independent public-entry audit instead found a deterministic presentation defect at the
320-pixel breakpoint: the persistent header action wrapped on the product, Video Studio, and
parent-company pages. Production cache revision 110 corrects that defect without changing auth,
pricing, acquisition attribution, native metadata, or private data.

A subsequent clean-origin signed-out startup audit found a separate deterministic byte cost: the
353,713-byte workspace mark loaded on registration deep links even though only the two horizontal
auth wordmarks were visible. Static markup, the final navigation runtime, and service-worker
precache were all contributing paths. Production cache revision 112 now defers every hidden mark,
removes it from precache, and retains intrinsic dimensions on every brand image. Clean mobile and
desktop checks against the exact deployment kept the email field focused, the primary action
available, and horizontal overflow at zero while every hidden mark remained unfetched. No auth,
session, attribution, pricing, customer data, or workspace-runtime sequence changed.

The cache-112 request trace also showed that four install-time image variants and one unused dark
wordmark were still downloaded by service-worker installation, even though the live registration
document needed only the 192px favicon and horizontal light wordmark. Cache revision 113 leaves all
files and manifest references intact but removes those optional images from mandatory precache;
the default worker path still caches any one that a browser later requests. A fresh-origin trace
proved the dark wordmark plus 180px, 512px, and 1024px images stayed unrequested, deferring another
647,855 bytes while the 192px favicon loaded normally. Together with the hidden workspace-mark
correction, clean signed-out registration now avoids 1,001,568 bytes that revision 110 fetched
before the user interacted, without changing install metadata, auth, or offline application code.

The 2026-08-28 trailing 24-hour review found a new transient database boundary: 28 `/api` 5xx
failures split between a 17-response `503` cluster affecting sync and credit-status routes and 11
earlier database-connection failures that included login and scheduled work. Current Supabase API
traffic had recovered to HTTP 200, and the Postgres log review did not show a corresponding
connection or resource-exhaustion cluster. Production 5.9.70 therefore adds bounded retries only
for idempotent Data API reads, keeps all writes and RPC calls on one server attempt, and emits only
categorical retry diagnostics. The exact deployment serves cache revision 104 and had no runtime
error or warning/error/fatal log in its initial window. Organic recovery versus recurrence remains
to be observed; no production fault or customer record was manufactured for proof.

A project-wide production runtime scan covering the trailing 24 hours on 2026-08-27 found no
runtime error clusters, no error/fatal/warning log entries, and no 5xx responses. The status
breakdown contained 1,841 successful 200 responses and five expected 401 responses from explicit
unauthenticated release probes against the disabled Crump Code, disabled Crump Voice, and private
Project attachment routes. A later user report exposed a client-visible login failure despite four
successful login responses and persisted sessions. Immediate confirmation probes were
unauthenticated before reaching the session table, which justified the 5.9.29 cookie/handoff
repair. The repaired deployment returned only 200 responses and no runtime error cluster or
warning/error/fatal logs in its inspected release window.

The 5.9.31 Project-resume release then rendered real owner-linked conversation metadata on desktop
and mobile without exposing message content or generating a synthetic retention event. Its inspected
deployment window showed no runtime error cluster, informational logs only, 41 successful 200
responses, and one expected 401 from the explicit unauthenticated ownership probe among the
reported status groups.

The 5.9.32 acquisition release then added two evidence-backed public entry pages without changing
authentication, billing, or private data. Production served both clean URLs, both page-specific
social cards, and the six-URL sitemap with 200 responses. The inspected deployment window reported
no runtime error cluster and only successful 200 responses. The comparable external cohort was zero
before release, so delivery is verified while acquisition lift remains unproven.

The 5.9.33 accessibility release then corrected 17 observed mobile contrast failures across the
homepage and résumé page. Both production pages now score 100 for accessibility with zero contrast
failures while retaining 100 performance, best-practices, and SEO scores in the verification runs.
Phone-size browser checks found no overflow or console issue, production health returned 5.9.33,
and the inspected deployment window reported no runtime error cluster and only 200 responses.

The 5.9.34 workspace accessibility release then removed the app-wide user-zoom lock and gesture
blockers while preserving horizontal-drift and mobile-input safeguards. The local registration
state moved from accessibility 93 to 100, and the local signed-out and production registration
states also scored 100 with zero contrast failures. Production health returned 5.9.34; the inspected
deployment reported no runtime error cluster, warning/error/fatal log, or 5xx response. Exact signed
device zoom and zoomed-layout usability remain store gates.

The 5.9.35 Crump Code review release then added the missing human control surface without enabling
the provider. Production health and six release assets returned 200, the Create entry remained
hidden behind configured-plus-entitled server state, and the inspected deployment had no runtime
error cluster, 5xx response, warning/error/fatal log, or `/api/code` request. Local browser evidence
proved the disabled state and the review-confirmation gate without creating a production task or
charge.

The 5.9.36 signup-guidance release then corrected a reproducible pre-submit clarity defect without
changing authentication. A short-phone browser check proved independent length/letter/number states,
polite status text, post-review invalid state, no horizontal or vertical overflow, and an above-fold
primary action. Production health and the app/release assets returned 200; the inspected deployment
had no runtime error cluster, warning/error/fatal log, or 5xx response. No production signup event
was generated, so delivery is verified while conversion impact remains unproven.

The 5.9.37 registration-handoff release then corrected the next reproducible activation defect:
successful registration no longer loses its verification instruction after 1.8 seconds. The
persistent state keeps the destination email, resend recovery, and sign-in continuation visible,
including when the account exists but initial email delivery fails. Production health and the live
shell/controller/stylesheet/service worker returned 200; the inspected deployment had no runtime
error cluster, warning/error/fatal log, 5xx response, or registration request. CI plus both hosted
unsigned native compiles passed. No synthetic account or event was created, so outcome remains
unproven.

The 5.9.38 first-workspace release then removed the nonessential display-name commitment after
verification and terms acceptance. Terms enforcement and server persistence remain unchanged; the
workspace now starts first and offers an optional, dismissible personalization prompt. Production
health and the changed release assets returned 200; the exact deployment had no runtime error
cluster, warning/error/fatal log, 5xx response, or signup/profile/terms/account/activation request.
CI plus both hosted unsigned native compiles passed. The owner's remembered-device path worked;
fresh credential-entry proof after sign-out remains pending. No synthetic account or event was
created, so activation impact remains unproven.

The 5.9.39 first-action release then corrected a deterministic slow-load race. The launchpad wired
Projects and Video before their product runtime, while a single 120-millisecond retry could expire
silently after already recording starter intent. A delayed local browser proved the old failure and
the event-driven correction, including visible busy state, explicit failure feedback, and latest-
choice behavior. Production health and the live readiness/cache assets returned 200; the exact
deployment had no runtime error cluster, non-informational log, or 5xx response. CI plus both hosted
unsigned native compiles passed. No production click, account, or synthetic event was created, so
activation impact remains unproven.

The 5.9.40 first-prompt release then corrected two browser-reproduced composer defects. Research
and Image erased an existing draft, while programmatic scaffolds did not emit the input event that
drives active/send/resize state. The corrected path preserves and prefixes the draft once, aligns
the real DOM state, and stops an exact bare scaffold before usage or chat mutation. File selection
and starter measurement remain unchanged. Production health and live composer/cache assets
returned 200; the exact deployment had no runtime error cluster, non-informational log, or 5xx.
CI plus both hosted unsigned native compiles passed. No production prompt, account, usage check, or
synthetic event was created, so activation and cost outcomes remain unproven.

The 5.9.41 authenticated-entry release then corrected a second deterministic availability
boundary after credentials or a saved session had already been accepted. The client awaited an
unbounded full-state sync before routing, so a stalled sync could strand login on a disabled button
or leave restored-session entry blank. The corrected path opens the account-scoped shell first and
uses the existing non-blocking, server-authoritative synchronizer afterward. A credential-free
browser fixture proved both old failures and both corrected paths. Production health and the live
shell/controller/cache assets returned 200; the exact deployment had no runtime error cluster,
warning/error/fatal log, or 5xx. CI plus both hosted unsigned native compiles passed. The fixture
made no production write; fresh owner credential-entry proof remains pending.

The 5.9.42 continuing-work sync release then corrected the same unbounded-network class at the
shared persistence boundary. A stalled push could leave message delivery or `Keep in a Project`
waiting forever even though the pending work was already safe to queue. The corrected manager
bounds fetch plus body parsing, retains the account-scoped queue on timeout/network failure, and
returns a retryable result. A credential-free browser fixture proved the old disabled state and the
corrected enabled retry with exactly one queued save. Production health and the live versioned sync
asset/cache returned 200; the exact deployment had no runtime error cluster or warning/error/fatal
log. CI plus both hosted unsigned native compiles passed. No production login, chat, Project,
account, payment, or synthetic event was created, so retention impact remains unproven.

The 5.9.43 first-message fixture correctly proved the early `app.js` fallback, but a subsequent
steady-state audit found the dynamically loaded `crump-5.0.js` primary runtime replaced Send with an
unbounded path. Its evidence is retained with corrected scope rather than overstated as full delivery.

The 5.9.44 first-reply recovery release closes that gap with one shared transport across both
runtimes. Usage preflight, acknowledgement, reply, and body parsing are bounded. After a lost reply
connection, the client polls an authenticated, owner-filtered, non-cacheable job route and reuses the
persisted server answer. A primary-runtime loopback fixture proved reply recovery plus a reusable
second Send, and a separate acknowledgement stall proved visible tap-to-retry completion. Production
health and live versioned/network-first assets returned 200; CI and both hosted unsigned native
compiles passed; the one-hour production scan had no warning/error/fatal log. No production login,
message, generation, Project, account, payment, or synthetic event was created, so activation impact
remains unproven.

The 5.9.45 authentication request recovery release extends that bounded-response standard to account
entry and recovery. Registration restores its action with truthful guidance when the outcome cannot
be confirmed, and web login reconciles a session that was already issued before the response stalled.
Credential-free loopback fixtures proved both paths. Production health and versioned/network-first
assets returned 200; CI and both hosted unsigned native compiles passed; the one-hour production scan
had no runtime error cluster or warning/error/fatal log. No production login, account, event, message,
Project, or payment was created, so fresh owner credential entry remains the final human proof.

The 5.9.46 signup milestone release corrects the next measurement boundary without changing the
signup product. A valid autofill-only submission previously skipped `SignupCredentialsReady` while
still emitting `SignupSubmitted`; the real-controller loopback fixture now proves the full ordered
sequence once for both untouched autofill and normal typing. Production health, the live versioned
controller, and service-worker cache revision returned 200; CI and both hosted unsigned native
compiles passed; the initial production scan had no runtime error cluster or warning/error/fatal log.
No production signup, account, event, login, message, Project, or payment was created, so conversion
impact remains unproven.

The 5.9.47 verification-link recovery release then corrected a second-click/security-scanner dead
end without weakening the single-use server token policy. The failed return now focuses the email
field, exposes the existing resend control, and offers sign-in when verification may already have
completed. A real-controller loopback fixture proved the pre-fix dead end and generic recovery result.
Production health and the live app/controller/service-worker assets returned 200; CI and both hosted
unsigned native compiles passed; the initial one-hour scan had no runtime error cluster or
warning/error/fatal log. No production signup, account, verification, event, login, message, Project,
or payment was created, so activation impact remains unproven.

Production 5.9.48 made the successful password-reset return durable, and 5.9.49 then closed the
reproduced slow-mobile confirmation plus sleeping-PWA update gap. A real-runtime fixture proved the
old three-check session failure and the corrected fourth-check workspace entry. The release also
replaced identifier-bearing upstream request logs with identity-free categorical outcomes. Hosted
CI and both native verifiers passed; fresh owner credential proof remains pending after the required
one-time restart of a PWA page that was already asleep before the new listener existed.

Production 5.9.50 then removed five expected protected 401s from every signed-out startup. The full
shell made only one successful session check while signed out, then hydrated every protected surface
after a mocked server-confirmed account. The fresh production browser repeated the one-request,
zero-failure result with `r84`; its deployment had no error/fatal log. This verifies a cleaner,
cheaper, and more observable activation boundary, not improved conversion.

Production 5.9.51 then removed two redundant synchronization owners found in the authenticated and
offline browser traces. Restored-session and fresh-login entry now run one ordered convergence
sequence; a stalled initial pull cannot be bypassed by a delayed starter push; offline startup makes
no sync request until one reconnect sequence. The production release served `r85` with one successful
signed-out session request, no protected call or browser error, and no runtime error cluster or
error/fatal deployment log. Owner credential-entry proof remains the final real-device auth gate.

Production 5.9.52 then closed the remaining network-stall boundary in the primary durable-value
action. The Project create/attach response is bounded, a completed save no longer waits for a
secondary list refresh, and an uncertain retry returns the existing owner-scoped Project instead
of creating a duplicate. The real browser fixture recovered twice from stalled responses and
completed the success path with an accurate accessible action and zero browser errors. Production
serves `r86`; hosted CI and both unsigned native source builds passed, and the inspected deployment
has no severe log or runtime error cluster. Legitimate Project conversion and later return remain
unobserved.

Production 5.9.53 then closed the corresponding return-to-work read boundary. Project discovery,
saved conversations, private notes, and response-body parsing are bounded independently; each
surface exposes its own reusable Retry action, and stale note results cannot cross a Project switch.
The real browser fixture proved Project-list, conversation, note, and body-stream stalls plus the
complete successful continuation with zero browser errors. Production serves `r87`; hosted CI and
both unsigned native source builds passed, and the exact deployment has no error/fatal log or
runtime error cluster. Legitimate Project return and retention lift remain unobserved.

Production 5.9.54 then corrected a privacy defect found by widening the runtime review beyond
application-owned auth messages. Serverless host logging could restore `httpx` INFO records after
app import, exposing full Supabase filter URLs with opaque session hashes and internal row IDs. The
new handler filter is reapplied before every request, raw database details are no longer logged, and
Ask Crump's categorical outcomes remain visible. A fake-cookie production probe forced a real
Supabase lookup: neither the fake hash nor `HTTP Request:` appeared, while the identity-free auth
outcome did. Production serves `r88`; hosted CI and both unsigned native builds passed, and the
exact deployment has no warning/error/fatal log or runtime error cluster.

Production 5.9.55 then separated an overnight transient database 503 from the current PWA login
path. One older-deployment login and nearby background jobs recorded the same upstream connection
failure; the current release returned successful database-backed session checks, and the owner
completed a real phone/PWA sign-out and sign-in. A separate real-controller audit found every auth
view transition left focus on a hidden link or the page body. Centralized transitions now focus the
first actionable field, and pre-network browser validation leaves a persistent announced message
plus content-free outcome telemetry. Production serves `r89`; hosted CI and both unsigned native
builds passed, five post-release database-backed probes returned 200, and the exact deployment has
no warning/error/fatal log.

Production 5.9.56 then closed the deterministic gap between high-intent organic entry and the
first creation workspace. Capability-page CTAs previously retained acquisition/location/plan but
discarded whether the visitor chose a document, presentation, résumé, or video. The allowlisted
category now survives authentication for 24 hours, opens the exact existing non-generating
surface, records one content-free continuation event, and clears only after acknowledgment. All
four immediate paths plus a signed-out-to-authenticated return passed in the real controller with
zero browser errors, and all four production pages passed phone-size CTA/overflow/console checks.
Production serves `r90`; hosted CI and both unsigned native builds passed. The exact deployment
also exposed intermittent background sync transport 503s already present before the release. The
account-scoped client queue remained intact, the next observed sync returned 200, and Supabase
reported `ACTIVE_HEALTHY` with matching database requests returning 200. The transport pattern
remains a reliability signal; no creation-intent conversion lift is claimed yet.

Production 5.9.57 then closed the next deterministic résumé-delivery gap. The public résumé
handoff and in-app `RÉSUMÉ · CV` outcome selected DOCX but discarded the résumé purpose before the
request. A fact-only experience/skills brief therefore resolved to a generic business document
unless the user repeated “résumé.” The exact allowlisted purpose now survives send, sync, retry,
model guidance, and DOCX/PDF packaging; arbitrary values are dropped. A real package verified the
résumé profile and layout, and the controller fixture verified `{format: docx, purpose: resume}`
with zero browser errors. Production serves `r91`; hosted CI and both unsigned native builds
passed, the exact deployment's observed requests returned 200, and no release-window runtime error
group appeared. No artifact conversion or résumé quality lift is claimed yet.

### Current monetization checkpoint

A live Stripe reconciliation on 2026-08-27 found five active catalog products and no transactions,
active subscriptions, paid customers, gross volume, or balance. The single customer record is the
internal owner account with $0 spend. The Professional live price ID matches the production
fallback. With owner approval, both webhook destinations now use direct `www` URLs, preserve their
descriptions, API version, signing secrets, and original narrow event scopes, and have returned 200
to signed harmless replays. No price, product, customer, tax setting, payment, or secret was changed.

The active web credit, subscription, portal, billing-state, and payment-return transports now bound
stalled requests to 15 seconds, restore disabled controls with a retry message, and clear Stripe
return parameters after a stalled finalization. Delivery is verified without a production checkout;
the remaining revenue gate is the first legitimate customer purchase and provider reconciliation.

## Ranked execution backlog

### Completed P0 — Repair live Stripe webhook delivery before the first payment

**Evidence:** Stripe now sends subscription events directly to
`https://www.askcrump.com/api/stripe/webhook` and credit-purchase completion directly to
`https://www.askcrump.com/api/billing/credits/stripe-webhook`. Signed harmless event replays returned
200 from both destinations. The credits replay also proved the 5.9.27 environment-key compatibility
fix against production without rotating or displaying the signing secret.

**Outcome:** both direct canonical destinations are active with their original narrow allowlists:
three subscription events and one credit-completion event. The replayed event was an expired Checkout
Session, so neither handler performed a subscription or credit mutation. Exactly-once credit-grant
behavior remains covered by the automated handler suite and should be observed on the first real
credit purchase.

**Release gate:** passed with owner approval, before/after destination evidence, signed 200 responses,
restored allowlists, production 5.9.27 health, no route error cluster, and successful CI/Android/iOS
verification. Rollback is to restore the prior apex URLs, although Stripe would again classify their
307 redirects as failed delivery.

### P0 — Convert useful answers into continuing Project work

**Evidence:** two external accounts completed 14 successful AI jobs with no recorded failures, but
the external aggregate contains zero Projects and zero files, and no external activity occurred
after 2026-08-23. Release 5.9.22 exposed the durable-work action directly. Release 5.9.28 now names
the exact conversation on the most prominent return card instead of asking mobile users to resume
unknown work. Release 5.9.76 adds the missing acquisition boundary: a canonical public Projects page
and a fixed content-free signup/sign-in intent that opens the authenticated Projects destination
without creating a Project or starting generation. Commit `08aae8d` closes the remaining local
handoff mismatch: once a conversation is saved, **Open Project** now enters that exact named
workspace rather than making the user find it again in the index. Delivery is verified; a legitimate external
durable-value transition and later return remain pending.

**Outcome:** expose private Project continuity directly on the latest result as a one-click next
action. Synchronize and ownership-check the conversation, attach it to the selected Project or
create one, and record only a content-free durable-value milestone. Keep feedback optional and
referral sharing secondary.

**Release gate:** automated ownership, mapping, direct-action ordering, content-free analytics, full
release verification, production health, desktop/mobile UI checks, named resume, bounded
queue-preserving persistence, and recoverable primary first-message/reply delivery passed through 5.9.44. The
exact post-save destination handoff and its browser regression passed in commit `08aae8d`. The remaining
outcome gate is at least one legitimate external conversation-to-Project transition and
a later return. Do not infer a retention rate from a single user.

### P0 — Review the first complete artifact journey cohort

**Evidence:** artifact-journey instrumentation reached production on 2026-08-27. Its first
service-role production snapshot returned no rows, which is the correct pre-traffic baseline;
Ask Crump will not insert synthetic production events to make the report look populated.

**Outcome:** use real traffic to identify the largest request-to-package or package-to-download
drop by artifact category. Keep reporting limited to aggregate stage counts and rates—never
prompts, responses, filenames, URLs, customer data, or arbitrary error text.

**Release gate:** at least one real production request and a written reconciliation of requested,
packaged, packaging-failed, and downloaded counts. Treat a small first sample as operational
evidence, not a statistically reliable conversion benchmark.

### P0 — Complete the Crump Code activation gates

**Evidence:** the server and private schema provide public-repository task creation, bounded tool
use, isolated execution, patch generation, verification, state transitions, cancellation, and
approval records. Production 5.9.35 adds the Project-attached human review surface, server-enforced
run confirmation, cost disclosure, patch download, and cancellation checks before every next
expensive step. The Create entry and provider remain disabled, and no real production sandbox run
has occurred.

**Outcome:** complete a human-visible workspace, diff and verification experience, prove the live
runtime boundary, add durable orchestration for longer work, and measure quality against a fixed
benchmark before any parity positioning.

**Release gate:** the human-visible cost/patch/approval UI, server confirmation, and local
cancellation-before-next-step contract passed in 5.9.35. Remaining gates are an approved sub-cent
sandbox smoke test, production OIDC verification, live cancellation and expiry tests, failure
monitoring, rollback exercise, a real approval-boundary scenario, and an end-to-end benchmark suite.
Do not advertise Codex or Claude Code parity until measured tasks show comparable completion
quality and safety.

### P0 — Prove the first comparable continuing-work journey

**Evidence:** the 30-day external cohort contains three accounts and two verified accounts. Two
accounts completed 14 successful AI jobs, proving historical first-use activity, but external
activity ended before the first observed product-event traffic and did not resume afterward.
The external aggregate contains no Projects, files, shares, checkout, or paid events. The current
event recorder works for an internal production tester, so historical zero-event rows are a cohort
boundary—not proof that the old users never activated.

**Outcome:** observe a new, legitimate post-instrumentation cohort complete verification, start
useful work, keep it in a private Project or file, and return. Use moderated sessions to identify
telemetry gaps separately from real usability failures. Do not create synthetic backfill events.

**Release gate:** at least three consented end-to-end observations, content-free event
reconciliation, one shipped fix for the largest verified failure, and a new cohort review before
any acquisition spend scales.

### P1 — Close the organic acquisition loop

**Evidence:** the Search Console domain property is verified through the live DNS TXT record, and
Google is processing the property's first performance and indexing reports. Production 5.9.34
introduced four high-intent capability pages at clean canonical URLs and linked them from the
homepage and from each other. Production 5.9.76 adds the canonical Projects workspace page and
reciprocal discovery links; three evidence-led workflow guides bring the live Ask Crump sitemap to
ten public URLs. The protected growth and
artifact reports still show zero comparable external activity, so acquisition is the current
evidence-backed bottleneck. A read-only Search Console inspection on 2026-08-27 showed that the
homepage is indexed, while the presentation, document, résumé, and video pages are all unknown to
Google with no referring sitemap or page detected. The Submitted sitemaps table contains zero rows.
A live URL test reports that the video page is available to Google and can be indexed, proving that
technical crawlability is not the current block. The expanded sitemap is live and ready, but it is not
entered or submitted in Search Console. A second read-only review on 2026-08-28 found the Page
indexing report still processing and the Submitted sitemaps table still at zero rows. Direct live
checks confirmed HTTP 200 delivery for `robots.txt`, `sitemap.xml`, the homepage, and every use-case
page, with no response-level robots block. The public pages still expose unique canonicals,
metadata, structured data, and crawlable internal links. No additional code defect was found; the
remaining acquisition gate is the owner-confirmed Search Console submission. The Projects page has
not yet accumulated a Search Console indexing observation. Commit `dc02d85` then closed the
crawler/no-index contradiction for `/app`: crawlers may fetch the shell, while exact HTTP and
rendered `noindex,nofollow` directives keep it out of search results. The release preserved
public-page indexability, protected-route blocks, and the sitemap boundary; production evidence is
recorded in `docs/SEO_CRAWL_HYGIENE_RELEASE_2026-09-01.md`. The apex redirect remains 307 and no
Search Console action was performed.

**Outcome:** verified domain ownership, one canonical sitemap submitted, valid canonical URLs,
and indexed landing pages tied to privacy-safe account-creation attribution.

**Release gate:** Search Console verification, live sitemap delivery, clean canonical HTTP/browser
inspection, unique metadata, and a Google live crawlability test passed. Remaining gates are
owner-confirmed submission and Search Console acceptance of
`https://www.askcrump.com/sitemap.xml`, followed by an indexed-page coverage review after Google has
had time to crawl.

The authoritative action-time checklist is `docs/SEARCH_CONSOLE_RELEASE_GATE_2026-08-27.md`.

### P1 — Observe the new activation and referral funnel before scaling spend

**Evidence:** starter intent, activation, durable value, useful-result feedback, recent-work
continuation, response sharing, checkout, and paid status are now measurable. Production 5.9.24
also prevents a failed clipboard operation from being counted as a share and preserves the
content-free `referral` channel through account creation, but the comparable production cohort is
new and no legitimate referred activation has been observed. A production-only Vercel Web
Analytics read on 2026-08-28 showed 89 visitors, 269 page views, and 62% bounce over the trailing
seven days; 61 visitors reached `/app`, 21 visitors produced 34 `SignupIntent` events, and one
visitor produced one client `AccountCreated` event. Those anonymous aggregates span the
pre-instrumentation boundary and may include internal or automated visits, so they are not a
conversion rate. The last 24 hours showed 14 production visitors, 93 page views, three
`MarketingCTA` visitors with eight events, two `SignupIntent` visitors with 14 events, and two
`SignupStarted` visitors, with no
`SignupCredentialsReady`, `SignupSubmitted`, or `AccountCreated` event. Before 5.9.29,
`MarketingCTA` mixed account-creation and sign-in clicks; the release now records existing-account
traffic separately as `MarketingSignin`. The service-role comparable external funnel still
returned zero accounts at every stage in the latest refresh, and the aggregate artifact journey
returned no rows. The deterministic audit found unbounded account-entry and recovery requests;
production 5.9.45 now bounds those requests through parsing and safely reconciles a web session
issued before a login response stalls. No comparable external account has yet been observed after
the repair. A follow-on autofill fixture then proved that a valid submit could omit
`SignupCredentialsReady`; production 5.9.46 now guarantees the complete one-time ordered milestone
sequence without changing signup behavior. The release evidence is recorded in
`docs/SIGNUP_MILESTONE_DELIVERY_RELEASE_2026-08-28.md`. A failed or reused verification return could
still strand a legitimate user after an email scanner or second click; production 5.9.47 now exposes
the existing privacy-preserving resend path and sign-in guidance without changing token or auth
policy. Its evidence is recorded in `docs/VERIFICATION_LINK_RECOVERY_RELEASE_2026-08-28.md`.
The next 24-hour read showed 19 visitors, 109 page views, 42% bounce, and two direct desktop-Mac
`SignupStarted` visitors with no credential-ready or submitted event. That sample remains too small
and automation-prone for a signup decision. The deterministic capability-page audit instead found
that qualified creation intent disappeared at `/app`; production 5.9.56 now carries only the
allowlisted document/presentation/résumé/video category through auth, opens the exact existing
non-generating surface, and emits `CreationIntentContinued`. The release evidence is recorded in
`docs/CREATION_INTENT_HANDOFF_RELEASE_2026-08-28.md`.
The subsequent creation audit found that a selected résumé lost its purpose after choosing DOCX;
production 5.9.57 now preserves only the allowlisted résumé purpose through sync/retry, guidance,
and final packaging. The release evidence is recorded in
`docs/RESUME_PURPOSE_DELIVERY_RELEASE_2026-08-28.md`.
The next post-boundary 24-hour read showed 22 visitors, 145 page views, 41% bounce, seven
`SignupIntent` visitors with 17 events, two submitted/completed login visitors, and no
`SignupStarted`, credential-ready, submitted, or created-account event. All seven signup visitors
landed directly on `/app`; six were attributed to Facebook and their intent was unspecified. That
sample is directional and may include internal or automated traffic. A credential-free local audit
found a separate deterministic cold-entry defect: at mobile widths the desktop product manifesto
is intentionally hidden, leaving a socially deep-linked visitor with cost reassurance but no
concise product value before the credential fields. Production 5.9.71 corrects only that copy and
readability gap. The evidence is recorded in
`docs/COLD_SIGNUP_ENTRY_RELEASE_2026-08-28.md`.
A pre-launch read-only production audit on 2026-08-28 then verified the presentation and résumé
capability pages used by the staged organic campaign. Both pages exposed their intended editable-file
outcome and limitations, and every signup or sign-in destination preserved the correct creation
intent plus the incoming Facebook acquisition channel. A new credential-free, loopback-only fixture
now executes the real landing runtime and proves that presentation and résumé CTAs remain distinct,
sign-in is reported separately from signup, and the acquisition source survives the next landing-page
navigation without contacting production. The fixture reported zero browser errors; all 415 tests and
44 JavaScript validations passed. No social post, production signup event, account, or customer data
was created during the audit. Publication of the staged batch remains owner-confirmed at action time.

**Outcome:** a weekly operating review of account creation → workspace open → starter intent →
activation → durable value → useful outcome → return/share → checkout → paid.

**Release gate:** at least one fully elapsed D7 cohort, explicit denominators, internal accounts
excluded, at least one legitimate referral delivery-to-account-to-activation observation, and a
written decision for the largest observed drop-off. Treat small samples as directional rather
than statistically conclusive.

### P1 — Measure richer social share previews

**Evidence:** Facebook was the largest observed external referral family in the trailing seven-day
Web Analytics view (`m.facebook.com` 10 visitors and `facebook.com` eight), while every public page
still exposed the square app icon as its share image. That traffic is small and may include internal
visits, so it establishes a channel worth instrumenting—not a reliable conversion benchmark.

**Experiment:** production 5.9.25 gives the home, presentation, and document pages distinct
1,200-by-630 social cards composed from the canonical Ask Crump mark and restrained product copy.
The intervention changes only link previews; landing copy, signup behavior, pricing, and attribution
remain unchanged. Existing acquisition and signup events provide a privacy-safe onsite outcome.

**Decision rule:** observe for at least 14 days and at least 50 combined Facebook/social referral
visitors before comparing socially attributed `SignupIntent` reach with the pre-release directional
baseline. Keep the cards if qualified onsite intent improves without a material rise in bounce;
revise or revert if the preview attracts less-qualified traffic. Do not infer social click-through
rate without platform impression data.

### P1 — Prepare native store distribution without premature submission

**Evidence:** production 5.9.71 is healthy; the Android release source regenerates as build 50971
with API 36, the permanent package ID, generated assets, cleartext/backup protections, and a passing
native source verifier. Structured en-US metadata passes current field limits. A reviewed Node 22
lockfile now supports clean `npm ci`, a zero-vulnerability npm audit, and deterministic Android
preparation from an isolated worktree. GitHub run `33228342063` generated the 5.9.71 iOS project and compiled its unsigned Release
configuration on hosted macOS with no signing or upload credentials. GitHub run `33228342176`
generated the 5.9.71/build 50971 Android project under Java 21, passed the native and signing-control
verifiers, compiled `bundleRelease`, and confirmed a non-empty unsigned
`.aab`, also with no signing or upload credentials. Firebase, RevenueCat public keys/products,
signing credentials,
publisher-account state, reviewer access, signed builds, physical-device results, screenshots, and
console declarations are not yet verified. The internal Apple/Google listing copy, reviewer path,
and exact-build screenshot sequence now match the released Ask, Projects, Create, Library, and You
information architecture, with Research correctly presented inside Ask. A live store-review URL
audit also found that the prior apex fields incurred an avoidable HTTP 307 before reaching working
pages. The packet now uses direct-200 canonical `www` support, marketing, privacy, privacy-choice,
and deletion routes, and the verifier fails if those exact destinations drift.

**Outcome:** produce exact signed Android and iOS candidates with truthful listings, reviewer access,
privacy/data-safety reconciliation, native purchase restoration, AI reporting, deletion, push,
accessibility, and reliable core workflows proven in internal testing.

**Release gate:** resolve every platform blocker in
`docs/STORE_READINESS_AUDIT_2026-08-27.md`, review the final signed-build packet with the owner, and
obtain explicit per-platform approval before submission. Never claim store availability from source
readiness alone.

### P1 — Reorganize the product experience before store screenshots

**Evidence:** Ask Crump's current interface accumulated navigation, product, polish, library, and
legacy compatibility layers as capabilities expanded. The product now needs a calmer hierarchy
before native screenshots and acquisition campaigns lock in the existing structure. This is a
product-direction decision, not evidence that the working experience should be discarded.

Release 5.9.30 completes the first staged slice: one labeled five-destination model on desktop and
mobile plus a non-generating Create chooser. The migration map and rollback are recorded in
`docs/PRODUCT_REORGANIZATION_MAP_2026-08-27.md`. Production and hosted native verification passed;
real-user task-flow evidence remains open before final screenshots.

Production 5.9.76 now closes the next activation gap in that reorganization. The replayable workspace
guide explicitly teaches that a named Project opens its own dedicated workspace, and every guide step
can hand the person into the corresponding real destination without generating content or spending
credits. The signed-in production path from You → About → Replay workspace guide → Projects opened
the real owner-scoped Project index with zero browser errors. All 479 regressions, 45 JavaScript
validations, production preflight, native web bundling, and store-metadata source checks passed. The
decision and release evidence are recorded in
`docs/ACTIONABLE_WORKSPACE_GUIDE_RELEASE_2026-08-29.md`.

Commit `19a0b93` completes the next hierarchy cleanup: Intelligence owns reasoning, memory/privacy,
live knowledge, and review; Create owns creation; the composer plus button owns attachments; and the
retired Web/Image/Code quick controls and hidden mode strip no longer ship. Crump Code is modeled as
its own future destination, not as an Intelligence or Create tool, but remains hidden until its
independent production gates pass. Conversation privacy is now durable across signed-in devices.
The release evidence is recorded in
`docs/INTELLIGENCE_ARCHITECTURE_PRIVACY_AND_IOS_ICON_RELEASE_2026-08-30.md`.

**Outcome:** organize everyday work around Ask, Projects, Create, Library, and You. Keep Research as
an intelligent mode within Ask; group documents, presentations, images, manuscripts, and video under
Create; and expose Code as a separate gated destination only after its independent readiness gates
pass. Preserve every account, conversation, Project, file, entitlement, and stable deep link. Use
restrained black/charcoal/gold styling, clear empty states, accessible motion, and consistent mobile
and desktop navigation.

**Release gate:** owner-approved information architecture and wireflow; a route/capability migration
map; staged implementation behind a rollback path; automated regression coverage; keyboard,
screen-reader, reduced-motion, and responsive checks; real-task usability review; production
reliability verification; and final store screenshots only from the exact signed candidate. Do not
begin a ground-up rewrite or major architecture change from visual preference alone.

### P2 — Prove the advertising creative system

**Evidence:** A local frame-by-frame review on 2026-08-29 covered all five owner-supplied MOV
candidates without uploading or publishing them. Individual shots showed useful atmosphere, but
every finished clip failed at least one acquisition gate: generated branding dominated the short
clips, the two longer files were letterboxed montages without a product throughline, and none
demonstrated a clear Ask Crump user outcome. The decision and reusable moments are recorded in
docs/VIDEO_CREATIVE_QA_2026-08-29.md. A new six-frame, unbranded, 9:16 “Work That Continues”
storyboard now provides a consistent protagonist, product story, real-world outcome, post-production
brand area, motion limits, voiceover, delivery specification, and release checklist. Its source
frames and handoff are recorded in docs/ASK_CRUMP_AD_STORYBOARD_2026-08-29.md.

**Outcome:** a restrained campaign library organized by hook, audience, duration, CTA, and funnel
stage, with branding added in post to prevent generated-logo distortion.

**Release gate:** creative QA, licensed audio/visual provenance, mobile-safe text, platform-native
aspect ratios, one measurable CTA, and controlled tests against activation—not view count alone.

## Next operating decision

The 2026-09-01 production measurement refresh showed **95** anonymous visitors over the selected
seven-day window, **16** `SignupIntent` visitors, **2** `SignupStarted` visitors, and no
credentials-ready or submitted signup event. The privacy-safe service-role comparable cohort still
contained zero production accounts, including with internal accounts allowed. That is a real
observation boundary but not a defensible conversion rate: owner/QA traffic is present and the
sample is below the existing 14-day/50-social-referral decision rule. Keep authentication stable,
observe legitimate attempts, and diagnose a consented failure sooner if one appears.

Treat desktop `/app` RES 79 as the pre-release performance baseline. Recheck after at least seven
full post-release days or 300 post-release desktop performance events, whichever comes later, and
compare the route-level score without mixing in the already-100 public routes. Do not infer a
real-user improvement from the controlled loader fixture alone.

Use the prepared canonical profile destinations only after owner confirmation:
`https://www.askcrump.com/?acquisition=facebook&source=profile-link` and
`https://www.askcrump.com/?acquisition=instagram&source=profile-link`. Treat the profile edit and
launch batch as one measurable social release, then observe legitimate profile-link visit →
signup → activation separately from the historical mixed-instrumentation window.

Observe legitimate capability or Projects CTA → auth → `CreationIntentContinued` → starter intent or
Projects workspace → durable value delivery, with the résumé journey retaining the selected purpose
through first download. Measure
whether bounded read recovery converts the intermittent database-transport pattern into categorical
retry warnings rather than final customer-visible `503` responses; do not change the shared
database boundary without a post-release recurrence. Submit the live canonical sitemap after owner
confirmation, allow the
social-preview experiment to reach its minimum
observation window, then obtain the first consented post-instrumentation account, durable-value,
return, referral, and artifact-journey observations. Observe the first real checkout and reconcile
Stripe with Ask Crump entitlement/credit state. Observe the 5.9.71 `SignupIntent` →
`SignupStarted` boundary for at least 14 days and 50 socially referred visitors, or diagnose the
next legitimate post-boundary attempt sooner if consented evidence is available. Future cold social
links should normally land on the public product or relevant capability page before signup unless
the campaign itself provides sufficient product context. Do not rewrite authentication from small,
anonymous aggregates. Keep both new provider foundations off. Do not enable Crump Code until the real
sandbox/OIDC test, review UI, monitoring, and benchmark gates pass. Do not enable Crump Voice
until its disclosure, key, voice rights, and playback tests are approved.

The launch packet is specified in `docs/SOCIAL_LAUNCH_BATCH_2026-08-29.md`. A current read-only
channel audit found that the workspace-positioning message is already live on Facebook and
Instagram, so the packet now prevents duplicate publication and retains only the presentation
outcome as the two-post remainder. Facebook still exposes untagged public links, Instagram does not
expose a clickable external link, and the verified Search Console property still lists zero
submitted sitemaps. The packet now names the exact privacy-minimized profile destinations, the
canonical sitemap, the two remaining contextual posts, and one action-time approval phrase.
Publication, profile edits, sitemap submission, and the dedicated ChatGPT progress update remain
owner-gated.
